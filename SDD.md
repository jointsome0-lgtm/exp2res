# Exp2Res — System Design Document

**Version:** 0.3  
**Status:** Implementation-ready  
**Project:** Exp2Res — Experience to Self-Assessment to Verified Bullet Pack
**Primary goal:** Build a local-first, provenance-heavy self-assessment system from owner-controlled experience evidence that automation cannot rewrite.
**Secondary goal:** Generate job-targeted verified bullet packs from the same evidence model without unsupported claims; a full resume document model is deferred to a post-mirror iteration.
**Primary user:** The developer using the system to understand himself honestly, orient through real experience, and optionally export a verified bullet pack for a specific vacancy.

---

## § Index

Section numbers are stable: issues and the Decision Log cite them as `§13` / `§13.2`. Never renumber. New sections take the next free number or a sub-number; update this index when sections change. Retired numbers are never reused: §12.1–§12.3, §12.5, §12.6, §12.9, §12.12 (2026-07-04, derivable tables replaced by §12's derivation rules from §11); §12.7, §12.8, §12.10, §12.11 (2026-07-04, issue #2 — Contradiction, GapQuestion, JobDescription, ResumeBranch got §11 models, their DDL is now derived); §13.2, §13.9 (2026-07-11, issue #19 — evidence recording folded into §13.1 and relevance matching into §13.10).

Layout: this file is the map. Each top-level § lives in `spec/NN-slug.md` (file name starts with the § number); the Decision Log lives in `DECISION-LOG.md`. Point reads: open the § file. Full pass: read `spec/` files in index order.

- §0 Historical Change Note — recentering: mirror first, resume is a secondary export
- §1 Executive Summary — evidence → facts → signals → assessment → single-pass verifier gates → optional verified-bullet-pack export, with a full resume document deferred post-mirror
- §2 Product Framing — weak framings to avoid; mirror-first framing and the V1 verified-bullet-pack boundary
- §3 Core Purpose — orientation, not impressiveness
- §4 Goals and Non-Goals — product/cognitive goals, V1 verified-bullet-pack scope and closed companions, deferred full resume document model, and forbidden inflations
- §5 Core Principles — truth over comfort; raw records append-only to automation and deletable by their owner, whose deletion authority also covers job descriptions and a whole-workspace privacy operation; whole-record correction displacement and recompute lifecycle via §13.3 effective-set semantics; recorded_at ≠ occurred_at; no precision/ownership inflation; contradictions first-class; explicit promotions and verifier-gated external projections
- §6 System Boundaries — source-agnostic activity-domain intake with selfos-side Tick-like mapping and learning knowledge routed through Atlas, Atlas knowledge-state snapshots with adapter-owned mapping and no semantic promotion, GitHub with upstream-only repository selection/access/authentication/fetching, and the verified bullet-pack export projection
- §7 High-Level Architecture — pipeline diagram
- §8 Runtime Architecture — Python, Typer, SQLite, Pydantic; CLI-first, with a one-business-writer/many-reader workspace advisory lock, WAL transaction discipline, next-writer reconciliation of abandoned managed-output publication siblings, `secure_delete` on every connection, destructive-flow WAL truncation, purge-only `VACUUM`, and database compatibility and migration owned by §12.14
- §9 Domain Model — ontology, actor-scoped raw immutability, current versus superseded derived generations, snapshot-anchored resume branches, append-only verifier-attempt history, claim kinds, confidence layers, evidence strength, and evidence-to-confidence calibration including studied/learning-scoped Atlas snapshot evidence and scoped displaced-record support for corrected imports
- §10 Enumerations — canonical Literal aliases including Atlas snapshot entry/strength values, plus temporal and general confidence orders, typed verifier targets, typed JD requirement kinds, typed GitHub owner attribution, managed-output kinds, and CLI result statuses
- §11 Pydantic Domain Models — canonical strict extra-forbid validation, field authorship, lifecycle-only mutation, and hash-serialization policy; bounded inert service/importer metadata; boundary size/count/depth and text-hygiene limits with offset-aware datetime acceptance, ISO calendar anchors, named-only NFC/default-case-fold identity, no implicit generated-prose normalization, and POSIX path shapes; lifecycle-managed entities with service-assigned, opaque, immutable, non-reused per-table IDs; correction-link deletion re-rooting with §13.3 displacement semantics and a deferred human-only private-marker reservation at §15 input assembly; storage-level production-provenance boundary; typed assessment scope/unknown/counterevidence storage; append-only VerificationFinding history; exact resume provenance; and ParsedJD requirements with stable IDs
- §12 SQLite Schema — offset-preserving ISO 8601 datetime TEXT with UTC-instant comparison; per-entity-table `TEXT PRIMARY KEY` identity, derived typed JSON storage with same-policy/limit fail-closed hydration, normalized fact provenance, per-row producing-run and atomic-generation provenance, transactional reference and correction-lineage evidence-selectability validation, JD-dependent deletion ordered so no FK blocks either point-deletion operation, a partial unique exact-name backstop for current named resume branches behind Stage 10's case-folded replacement identity, and normative DDL for fact_sources, parent-linked privacy-safe processing-run execution identity with per-call llm_calls telemetry and §15.10-owned stable runtime failure codes, and append-only schema_meta with the §14.16 fresh-history exception; fail-closed compatibility plus verified-backup, single-transaction migration
- §13 Pipeline Specification — 10 active stable-number stages with run/generation traceability, deterministic whole-record correction displacement, effective-record and per-fact governing-record resolution, scoped survival and universal prose-free §15 projection of non-manual displaced-record support, conservative occurred provenance, LLM-backed Stage 4/8 contracts, atomic durable verifier findings, fact-mediated gap answers, typed assessment content, snapshot-anchored isolated bullet generation with deterministic post-generation selection/order/exact-dedup checks, closed versioned assessment and verified-bullet-pack companions with typed evidence closure and byte-identical rendering, workspace-locked mutations, one named three-trigger stale-export invalidation rule, manifest-backed ID-derived managed-output paths with temp-sibling atomic publication, render-input-bound currentness, recoverable crash cleanup, owner-private modes, and canonical no-follow containment, Stage 3–5 lifecycle recompute grouped under non-stage 13.13 orchestration telemetry, privacy-first raw-log reset, and dependency-purging JD deletion with ID-keyed managed-output/backup removal and retained-telemetry content-hash redaction
- §14 CLI Specification — sole command-form authority with the §8.1 workspace business-writer/read-only classification and a global runtime contract for nearest-parent discovery, configuration precedence, non-interactive controls, stable exit taxonomy, a versioned JSON result envelope with complete typed integration-import outcomes and manifest-keyed export results, and workspace-timezone-only local-time resolution with fail-closed DST gaps/folds; idempotent non-destructive init, pre-business-I/O compatibility, read-only database status, explicit migration, activity-domain and Atlas-snapshot local-payload import mappings, self-contained correction capture with whole-record restatement framing, raw-log owner deletion, extract/recompute, generation with explicit selectors, persisted project scope targets, path-independent branch names with NFC-folded replacement/selection identity, the product-facing `bullets generate|verify|export` flow, verifier findings, read-only run/finding inspection, JD inspection/deletion, whole-workspace purge, and assessment export
- §15 LLM Contracts — full structured extractor/writer/verifier plus Stage 4 detector and Stage 8 parser I/O over effective correction-lineage content with universal prose-free displaced-record support descriptors and no displaced `RawLog` prompt objects; a normative §15.11 field-ownership matrix with echo-free outputs and deterministic service-side copies/derivations, strict service-owned-field rejection, deterministic size/structure and hard budget/context preflight, processing-run execution identity, §16.13 V1 language-scope binding, findings-only advisory-rewrite persistence, schema retry distinct from semantic results, bounded foreground transport retry, declaration-checked provider capability, whole-stage multi-call failure/cancellation semantics, privacy-safe progress/cost reporting, the versioned §15.12 isolated agent-runner protocol binding every agent-backed adapter with wrapper-provided canary-verified structural confinement, and the closed §15.13 V1 adapter lineup — default Codex CLI runner on `gpt-5.6-sol`, secondary Claude Agent SDK on `claude-opus-4-8`, required OpenAI-compatible direct transport — switchable by `[llm]` configuration alone with exactly one declared §29.2 credential form per adapter
- §16 Verification Rules — evidence/status gates and generated/source voice scoping; English-only generated and verified V1 output, byte-exact any-language sources, cross-language extraction, mixed-JD fidelity, and localized-output deferral; mirror, anti-flattery, ownership, metric, production, temporal, employment, identity, and diagnostic rules
- §17 Self-Assessment Report Format — deterministic section-to-claim selection and ordering for the status-labeled mirror, typed scope/unknown/counterevidence, precision-safe OccurredAt rendering, complete contradictions, origin-aware prose, and shared byte-identical Markdown rules
- §18 Verified Bullet-Pack Export Rules — isolated persisted bullets plus §13.12 closed companions, required assessment anchoring, typed JD requirement references, structural-only rendering, voice-scoped prose, status allowlists, export-fail conditions, and deferred full resume document model
- §19 Integration Contracts — source-agnostic activity-domain intake with selfos-side Tick-like wire/time mapping, Atlas knowledge-state snapshots on source-owned scales with trail/evidence references and no direct semantic promotion, and GitHub commits through closed acquisition-versioned envelopes with source-record identity and body-hash idempotency; full-SHA GitHub identity, typed provenance, conservative attribution mapping, idempotent duplicates, fail-closed conflicts, atomic bounded batches, complete typed outcomes, referenced-source digests, strict structural validation, POSIX-gated inert locators, and unchanged source prose
- §20 Suggested Repository Structure — placement principles + normative skeleton
- §21 Evals — 50 behavioral tests including closed version-1 assessment and verified-bullet-pack companions, golden-file byte-identical rendering, typed evidence-map round trips, deterministic exact-duplicate suppression, Markdown/temporal fidelity, and canonical `bullets` command-envelope coverage; versioned idempotent integration imports with fail-closed conflicts, all-or-nothing batches, rerun convergence, complete typed outcomes, and referenced-artifact integrity; source-agnostic activity-only ephemeris routing, calibrated no-promotion Atlas snapshots, and deferred loopback URL-only local views with first-class refusal and manifest-backed snapshot-ID unanswered-gap handoff; ID-keyed manifest-identified assessment/verified-bullet-pack export isolation, atomic no-follow publication and fail-closed validation, and dependent JD-output deletion, complete JD-deletion and workspace-purge privacy lifecycles, bounded foreground LLM transport, fail-closed budget/context preflight, and interruption/crash safety, deterministic temporal, language-scope, Unicode-identity, and POSIX-path behavior, deterministic machine-readable CLI runtime behavior, computable whole-record correction displacement, scoped-support survival, and prose-free downstream projection, strict typed bounded transport/hydration behavior, producing-run and atomic-generation provenance, durable verifier history and orchestration parentage, concurrent-process workspace isolation, fail-closed schema compatibility and all-or-nothing migration recovery, unique immutable non-reused entity identity, temporal provenance, typed JD references, Stage 4/8 contract discipline, voice scoping, typed assessment uncertainty, verifier input closure, deterministic scope selection, generated employment/identity/mirror behavior, instruction-like JD isolation, a prompt-injection threat-path matrix across extraction, detection, verification, and writer ingress with byte-exact source preservation and no undeclared authority, and structurally confined agent-backed runners with canary-proven read confinement and two-half fail-closed preflight
- §22 Implementation Plan — Phase 0–5 with definitions of done naming offline test groups, including the global CLI runtime contract, schema-version initialization, pre-business-I/O compatibility gate, the §15.12 agent-runner execution boundary from Phase 1, and the verified-bullet-pack Phase 4 boundary
- §23 End-to-End Demo — approximate-range retro log → complete typed facts → signal → claim → complete two-bullet verified pack plus closed companions
- §24 Acceptance Criteria — 54 V1 checks including closed version-1 assessment and verified-bullet-pack companions, golden-file byte-identical rendering, typed evidence-map round trips, deterministic exact-duplicate suppression, Markdown/temporal fidelity, and canonical `bullets` command-envelope results; versioned idempotent integration imports with fail-closed conflicts, all-or-nothing batches, rerun convergence, complete typed outcomes, and referenced-artifact integrity; domain-routed activity and knowledge-state imports with calibrated no-promotion, plus the deferred loopback-only local-view refusal and manifest-backed snapshot-ID unanswered-gap handoff contract; ID-keyed manifest-identified managed exports with atomic no-follow publication and fail-closed validation plus dependent JD-output deletion, complete JD-deletion and workspace-purge privacy lifecycles, bounded foreground LLM transport, fail-closed budget/context preflight, and interruption/crash safety, deterministic temporal, language-scope, Unicode-identity, and POSIX-path behavior, the deterministic global CLI runtime and machine-readable result contract, deterministic whole-record correction displacement, scoped-support survival, and prose-free downstream projection, one closed strict bounded interpretation per accepted object, producing-run/generation traceability, parent-linked lifecycle orchestration, durable verifier findings with telemetry-only failures, privacy-safe run identity, owner-deletion purge, one-writer/many-reader workspace locking, coherent snapshot reads, bounded contention and crash recovery, schema compatibility and verified atomic migration, the per-table primary-key identity and no-reuse contract, §21.49's prompt-injection threat-path matrix across raw capture, import payloads, JD text, and evidence context with no source mutation or undeclared authority, §21.50's versioned agent-runner isolation for every agent-backed adapter, and §24.54's config-only switching across the closed three-adapter §15.13 lineup with fail-closed externally-managed sessions
- §25 Risks and Mitigations — reference-only mitigations for resume-drift, flattery, punitive tone, overclaim, fail-closed LLM budget/context preflight with no input truncation, single-pass verifier gates, provenance corruption, verifier-gated integration projections, diagnosis, and retained private/stale derivations mitigated by raw-log, JD, and whole-workspace privacy operations
- §26 README Positioning — verified-bullet-pack intro and taglines; MIT license, stage-tracking README status per §22 phase progress, and deferred security-policy positioning
- §27 Key Invariants — reference-only index including typed JD/unknown references, occurred provenance, generated/source voice boundaries, and the local privacy/egress boundary
- §28 Final Design Statement — three layers that must never collapse
- §29 Security and Privacy — private local canonical state with owner-only workspace permissions and manifest-backed ID-derived managed-output containment/no-follow operations, loopback-only §30 view serving classified as local presentation rather than egress, explicit IANA workspace-timezone configuration, secret-free config carrying environment/keyring references or an adapter's fail-closed externally-managed CLI session with no in-config credential slot, exhaustive displacement-aware user-initiated LLM transit with bounded same-action retries, provider trust with declaration-only capability checks and no discovery probe, structurally confined §15.12 agent-runner execution for agent-backed adapters with wrapper-provided canary-proven read confinement, size/structure and secret preflight, POSIX-only Linux/macOS local paths with Windows forms rejected, ignore isolation including descriptor locators, prompt-injection bounds, a managed-data lifecycle inventory, and explicit physical-erasure/provider/external-copy limits
- §30 Local Views — deferred post-CLI, loopback-URL-only selfos embedding of the read-only-over-derived-state mirror and JD-to-bullet-pack views through existing §14 flows, with first-class export refusals and current unanswered gaps projected from the selected snapshot's closed manifest-backed `self_claims.json` without answer link-back
- Decision Log — dated one-line decisions with rejected alternatives

---

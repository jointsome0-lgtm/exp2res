Format: `YYYY-MM-DD — the decision in one sentence.` plus, where the decision recorded one, `Rejected: alternative — reason.`
Entries are one or two sentences; the full argument lives in the issue, the PR, and git history.
Entries are linted by `python3 scripts/check_decision_log.py --baseline 2026-07-15 --max-words 80 --warn-words 40 DECISION-LOG.md`;
entries on or before 2026-07-15 are a grandfathered baseline.

## Decision Log

- 2026-07-03 — The SDD stays one file navigated by the § Index; a physical split into a map plus per-§ files waits until after the structural dedup pass and must then be a mechanical, byte-reproducing commit. Rejected: splitting now — point reads already load only the needed section, and a split would migrate content about to be merged or deleted.
- 2026-07-04 — Structural dedup pass: §12 becomes derivation rules over §11 with normative DDL kept only where no Pydantic counterpart exists, §22 compresses to a phase → stages → definition-of-done table, and anti-flattery mantras consolidate into §16 with one-line references elsewhere; no version bump. Rejected: leaving duplicates in place — the cost is paid again in every polishing session and cross-copy drift grows.
- 2026-07-04 — Executed the deferred split: `SDD.md` is the map, each top-level § lives in `spec/NN-slug.md`, and decisions move to `DECISION-LOG.md`, with numbering and content unchanged and byte-for-byte concatenation verified. Rejected: a single file plus convention — conventions don't bind agents, structure does.
- 2026-07-04 — The § Index gains "retired numbers are never reused" plus the retirement registry, drops the stale header Date field, and requires a § edit to update its map line in the same commit. Rejected: a hand-maintained Date and per-§ retirement notes — a duplicate of git metadata that had already drifted.
- 2026-07-04 — Issue #1: §20 drops its 112-line speculative file tree for placement principles plus a normative skeleton, deriving module, test, and example names from §13, §21, §27, and §19 instead of pre-naming them. Rejected: keeping the tree — it restates those §§ at full-pass cost and drifts the moment code lands.
- 2026-07-04 — Issue #2: Contradiction, GapQuestion, JobDescription, and ResumeBranch get Pydantic models (§11.9–§11.12) and §12 derives their tables, leaving normative DDL only for the two non-ontology storage artifacts. Rejected: declaring the four storage-level-only — it saves no spec volume and leaves a third of the ontology without a domain model at the LLM boundary.
- 2026-07-05 — Issue #24: §20 principle 3 points job descriptions to §13.8 (Stage 8 — Job Description Parsing) instead of §13.11. Rejected: leaving the pointer for readers to infer — it sends `examples/` population to the wrong pipeline stage.
- 2026-07-05 — Issue #6: §10 is the canonical, mechanically extractable home for every enum value list used by §11 models, with §11, §13, and §27 reduced to named alias and rule references; no generator, linter, or registry file is introduced now. Rejected: model-local literals or prose restatements — they preserve multiple homes for one list and repeat the atlas drift failure.
- 2026-07-11 — Issue #11: §10 owns the ten-value `EvidenceStrength` taxonomy typing `EvidenceItem.strength`, and `EvidenceItem.evidence_type` is removed because source origin already lives on `RawLog`, with GitHub-commit evidence recorded at `commit_or_pr` as in §19.3. Rejected: a second `EvidenceType` taxonomy — it duplicates the strength and origin axes without a demonstrated use.
- 2026-07-11 — Issue #11: `fact_sources.support_type` is constrained to `direct` or `corroborating` independently of `EvidenceStrength`, and every fact requires a direct source. Rejected: an open string or reused evidence-strength values — either leaves an undefined value space or collapses source relationship into source strength.
- 2026-07-11 — Issue #16: `OccurredAt.precision` is the sole discriminator determining the legal `start`/`end` shape, and the redundant `OccurredAt.kind`, `OccurredAtKind`, and `occurred_kind` column are removed. Rejected: persisting both fields plus a legal-pair matrix — it stores derivable state with no V1 information gain.
- 2026-07-11 — Issue #16: `Confidence` types general confidence on `ExperienceFact`, `SelfSignal`, and `SelfClaim`, while `TemporalConfidence` is reserved for `OccurredAt.confidence`. Rejected: reusing `TemporalConfidence` because the members match — it conflates confidence in when something happened with confidence in what a claim asserts.
- 2026-07-11 — Issue #15: `OwnershipLevel` is a normative total order from `unknown` (weakest) through `led`, claim confidence stays independent, and verifiers fail closed when ownership language cannot be normalized. Rejected: making `unknown` incomparable — a verifier could not conservatively decide whether a known ownership phrase is an upgrade.
- 2026-07-11 — Issue #15: non-range `TemporalPrecision` values compare by weak-to-strong rank and normalized maximum uncertainty width, while ranges compare by actual width with approximate bounds weaker at equal width. Rejected: comparing raw enum positions — range precision depends on duration, so a fixed position misclassifies a two-day range against a month.
- 2026-07-11 — PR #30 review: zero-width ranges are illegal — `date_range`/`approximate_range` require `end` strictly after `start`, and an instant is `exact_datetime`/`exact_day`. Rejected: a precision-implied width for zero-width ranges — one moment would have two representations and §16.7's equal-width rule would be ambiguous.
- 2026-07-11 — Issue #26: every persisted entity except `RawLog` carries a system-assigned `created_at` at first persistence, `RawLog.recorded_at` keeps its §5.4 meaning, and §12 derives every entity timestamp without exception. Rejected: inferring creation from `processing_runs` — run metadata is guaranteed only for LLM calls.
- 2026-07-11 — Issue #19: a stage requires a §14 trigger and a downstream-referenceable persisted result, so evidence recording folds into Stage 1, relevance matching into Stage 10, and §13.2/§13.9 retire without renumbering, leaving ten active stages. Rejected: keeping paper stages or forcing exactly nine — either preserves the original contradiction or violates the same stage criterion.
- 2026-07-11 — Issue #20: §14 is the only home of command forms — noun-led generation groups take an explicit `generate` verb, required paths stay positional, selectors and context use named options — and §13 carries semantic trigger references only. Rejected: per-stage command copies or bare-noun generation — both preserve grammar drift surfaces already falsified in practice.
- 2026-07-11 — Issue #21: V1 keeps only the eight `EntryType` values with explicit capture or import producers — Atlas restricted to artifact references, GitHub to commits, `import file` a design-document importer — and six producerless values retire until they have contracts. Rejected: speculative importers for the retired values — they enlarge V1 to preserve unused enum members.
- 2026-07-11 — Issue #21: `llm_inferred` leaves `SourceType` and LLMs may neither create nor mutate `RawLog`; inference begins at `ExperienceFact` and stays attributable to processing runs. Rejected: an LLM raw-log producer — it would place derived interpretation in the immutable user and imported truth layer.
- 2026-07-11 — Issue #21: `export_claim` leaves `ClaimKind`, and resume bullets stay a distinct projection carried by source links and verification fields. Rejected: a constant-like `claim_kind` field on `ResumeBullet` — it adds no V1 distinction or verifier behavior.
- 2026-07-11 — Issue #22: V1 initialization creates only `.exp2res/` and `out/`; SQLite owns raw and evidence records while supplied files stay in place and their paths are stored as provenance. Rejected: copying inputs into managed `logs/` or `evidence/` directories — a second file lifecycle and ambiguous source of truth with no V1 consumer.
- 2026-07-11 — PR #31 review: the §15.5 assessment-verifier output carries `counterevidence` — contrary statements grounded in supplied sources, empty when none — so Stage 7 can persist `SelfClaim.counterevidence` through a validated payload. Rejected: keeping the field outside the verifier contract — no other flow produces counterevidence, leaving §11.6's field unfillable.
- 2026-07-11 — PR #31 review: `gaps answer` sets `GapQuestion.answered = true` together with `answer_log_id` in one transaction, and `answered` is true iff `answer_log_id` is set. Rejected: dropping the bool as derivable — §12 rule 4 already derives a NOT NULL column from it, and removing a §11.10 field exceeds this fix.
- 2026-07-11 — Issues #3 and #9: raw records are immutable to automation but owner-deletable — a correction appends a linked record and replaces one coherent current derived generation while preserving superseded history, while owner deletion hard-deletes the record, globally purges every derived generation, verifies managed-export removal with residual reporting, and rebuilds from surviving sources without letting cleanup or rebuild failure undo the database deletion. Rejected: warn-and-skip or selective graph deletion — V1 cannot prove a selective purge complete, so either can retain private derived text.
- 2026-07-11 — Issue #17: V1 keeps upper typed ID lists as JSON but resolves every target transactionally before persistence, while `fact_sources` uses non-null EvidenceItem links and derives raw-log provenance through `EvidenceItem.raw_log_id`; unresolved or inconsistent links fail the producing run atomically. Rejected: a generic link table or per-field tables — SQLite cannot enforce cross-table keys there, and neither has a V1 query need.
- 2026-07-11 — PR #34 review: `corrects_log_id` is a capture-time requirement, not a standing invariant — a correction orphaned by owner deletion is a valid state that roots its own lineage. Rejected: rewriting the orphan's `entry_type` or purging its lineage — the first is a system rewrite §5.3 forbids, the second deletes records the owner did not select.
- 2026-07-11 — PR #34 review: the §16.1 current-chain requirement binds current rows and verification and export inputs only, so superseded rows stay inspect-only history whose references legitimately point at superseded targets. Rejected: applying the chain rule to every persisted row — it would invalidate preserved history or force a purge on every correction.
- 2026-07-11 — PR #34 review: `gaps answer` copies the question text and reason into the answer's `RawLog.metadata`, so answers stay interpretable after regeneration, and links to regenerated questions are never re-created. Rejected: preserving `GapQuestion` rows through the reset or post-rebuild re-linking — one retains derived text the purge must remove, the other has no surviving row to match.
- 2026-07-11 — PR #34 review: the §15.2 fact-extractor input carries `RawLog.metadata` through unmodified, so §14.7 gap-answer question context reaches extraction. Rejected: requiring owners to restate the question inside their answer text — the system already holds the context and manual restatement is unreliable.
- 2026-07-11 — Issue #8: §16.11 is the sole operational home of `VerificationStatus` meanings and role-aware consumer allowlists, Stage 7 reduces snapshot status over all claims including a required `narrative_summary` claim that exactly carries the snapshot summary prose, assessment exports preserve visibly labeled uncertainty, and only `supported` claims and bullets become resume content while `ExperienceFact` loses its producerless status field. Rejected: denylist gates — they let unnamed unsafe states pass.
- 2026-07-11 — Issue #7: every `ResumeBranch` requires the exact current `AssessmentSnapshot` named by §14.10's required `--snapshot`, with no implicit latest default and branch, bullet, and source-claim consistency validated before persistence and export. Rejected: optional or inferred anchoring — it is ambiguous with multiple snapshots and lets the assessment and export layers collapse.
- 2026-07-11 — Issue #18: V1 review means Stage 7/11 verifier gating of assessment and resume projections, not an owner verdict on regenerated derived rows, and producerless `SourceType.user_confirmed` is removed. Rejected: durable owner confirm/dispute raw records — they need a new producer, verdict payload, cross-regeneration matching, and precedence rules, enlarging V1 into a review subsystem.
- 2026-07-11 — Issue #23: V1 removes `resolved`/`dismissed` state and `resolution_note` from contradiction rows — Stage 4 always replaces the complete current set, every current snapshot renders that set unfiltered, and prior detections stay superseded history outside the owner-deletion privacy reset. Rejected: resolve/dismiss commands backed by raw verdict records — regenerated contradiction IDs have no stable identity, so durable application needs semantic matching machinery.
- 2026-07-11 — Issue #27: V1 verification is one validated semantic pass per current claim or bullet — §15.1 retries only invalid structured output, findings and suggested rewrites return to the owner, and verifiers never invoke writers or mutate derived prose, so repair needs an explicit Stage 6/10 replacement generation. Rejected: an automatic writer-verifier repair loop — it needs orchestration and termination rules across four stages and conflicts with the superseding lifecycle.
- 2026-07-11 — Issue #10: resume generation uses one isolated §15.6 invocation per bullet carrying only that bullet's evidence and supported claims, typed JD requirements, exact provenance sets, and the writer-settable `ResumeBullet` fields. Rejected: batching bullet contexts or passing snapshot prose — it exposes cross-bullet inputs and recreates the unverified wording channel PR #35 closed.
- 2026-07-11 — Issue #13: fact time inherits the governing `RawLog` placement, permits only explicitly evidence-backed contained narrowing, and never exceeds in-context precision or governing temporal confidence, while Stage 5 consumes current facts, their evidence items, and contradictions with gap answers arriving only through Stage 3. Rejected: literal source-copy-only time or direct gap-answer inputs — the first discards stronger supplied evidence, the second bypasses fact provenance.
- 2026-07-11 — Issue #14: Stage 4 semantic detection and Stage 8 JD parsing are LLM-backed under appended §15 contracts, `ParsedJD` owns service-assigned stable typed requirement IDs, and matching stays internal to Stage 10. Rejected: unspecified deterministic semantics or model-authored parsed identity — each leaves an LLM boundary unvalidated or makes service failures model-retryable.
- 2026-07-11 — Issue #12: assessment unknowns are the complete current unanswered Stage 4 set in typed `AssessmentSnapshot.gap_question_ids`, `project` scope stores the §14.9 selector as `scope_target`, and §17 renders all claim counterevidence with visible status. Rejected: writer-selectable unknown strings or metadata storage — they permit uncertainty omission and duplicate gap state.
- 2026-07-11 — Issue #4: verification separates generated voice from source voice — structure is validated everywhere, every untagged LLM string is generated voice, and only typed byte-verified source segments escape §16.2–§16.10. Rejected: trusting quote-like text or scanning concatenated payloads without origin boundaries — either launders model prose as source or treats user text as a system claim.
- 2026-07-11 — PR #38 review: snapshot gap references are synthesis-time state — `gaps answer` never supersedes or fails a current snapshot, the answered-exclusion rule binds only the Stage 6 write boundary, and §17 renders a later answer as answered-since-synthesis context. Rejected: invalidating dependent snapshots inside the answer transaction — it destroys a still-honest mirror over evidence the pipeline has not extracted yet.
- 2026-07-11 — PR #38 review: `gaps answer` applies §13's managed-output invalidation to the exports it stales — enumerate, attempt removal, report residual paths — while superseding no row, so snapshot and branches stay immediately re-exportable. Rejected: leaving pre-answer artifacts in place — a current-looking export would keep presenting the question as open.
- 2026-07-11 — PR #38 review: Stage 4 consumes effective lineage evidence only — the raw logs and evidence items governing current extraction under §13.3 rule 10, with factless effective records still targetable — so content displaced by a selected correction is neither input nor target. Rejected: the complete retained raw and evidence set — the detector would resurrect exactly the conflicts the correction flow removed from the current generation.
- 2026-07-11 — PR #38 review: §16.2–§16.10 are owner-referential — parsed external-demand text stays generated voice for structure and parse fidelity, but preserved demand wording characterizes the vacancy, cannot make `jd add` unpersistable, and is never force-rewritten. Rejected: treating demand text as owner-claim voice — ordinary vacancy phrasing would force meaning-destroying rewrites of the employer's requirement.
- 2026-07-11 — Issue #41: each entity's full-shape example lives once at its producing contract (§15.2, §15.4, §15.9) and other examples elide repeated bodies to `<id: complete §NN Model>` placeholders defined in §15.1, with no normative change. Rejected: full objects in every input example — five copies made each §11 field change a five-site edit and doubled §15's read weight.
- 2026-07-11 — PR #42 review: a §15.1 placeholder denotes the complete persisted §11 shape — the producer example plus exactly the service-set fields its prose names — because producer examples are deliberately model-emitted subsets. Rejected: restoring a full persisted example per consumer input — it recreates the duplication #41 removed while §11 already carries the normative shape.
- 2026-07-11 — PR #42 review r2: §15.1's producer references are corrected (signal → §15.3, gap and contradiction → §15.8) and a behavior-bearing example object — one whose content the same example's output depends on — is never elided, so §15.8 shows its fact and raw log in full. Rejected: placeholder field-overrides for load-bearing content — inline deltas recreate the example divergence the notation prevents.
- 2026-07-11 — Issue #5: the owner-controlled local workspace is the only canonical persistence domain — only foreground user-started runs may transmit the exhaustive typed §15.2–§15.9 payloads to an explicitly selected provider, while every other network call, outbound telemetry, secret or ignored context, and instruction-following from source data is forbidden, and weakening this requires another log entry. Rejected: relying on the "local-first" header and provider abstraction — they leave data transit, prompt retention, secret reads, injection, and non-LLM egress unconstrained.
- 2026-07-11 — Issue #25: §21.27–§21.29 exercise employment-framing rejection, permanent-identity rejection, and unchanged passage of an uncomfortable evidence-grounded mirror claim, with matching §24.31–§24.33 criteria. Rejected: inferring coverage from §24.8/§24.10 and neighboring evals — none exercises these three §16 behaviors or proves unchanged prose.
- 2026-07-11 — PR #44 review: the §29.4 deny gate governs the source-acquisition channel, not every file open — the service's reads of §14.1 workspace internals are internal I/O, while those paths stay denied as locators and their content stays out of prompts. Rejected: a literal every-open gate — the first config load that selects the provider becomes self-denying and circular.
- 2026-07-12 — Issue #32: `EvidenceStrength` closes over V1 producers, retiring `weak_note`, `code_artifact`, `test_or_demo`, `external_feedback`, and `verified_outcome` because no §14 flow assigns them; a retired value returns only with its producing flow. Rejected: keeping them as documented reserved values — a closed Literal with unreachable members is the producerless-enum disease #21 removed.
- 2026-07-12 — Issue #58: evidence-to-confidence calibration is capability-based, not a total order (new §9.4) — each strength defines an evidential scope, deterministic ceilings and propagation caps bound `Confidence`, a candidate above a cap is invalid structured output rather than silently lowered, and strength never authorizes ownership, metric, production, temporal, or employment content. Rejected: a numeric or total strength order — the values are qualitatively incomparable and a rank invites cross-scope authorization.
- 2026-07-12 — Issue #40: `custom` retires from `AssessmentScope` as producerless, `--scope` explicitly accepts the remaining §10 values with `global` as default and `project` alone carrying a target, and a custom scope returns only with its command form and replacement-key semantics. Rejected: adding `--scope custom --target <label>` — a free-label scope duplicates project scoping without a distinct V1 consumer.
- 2026-07-12 — Issue #57: Stage 4 targets use the closed `DetectionRefType` alias (`raw_log`, `evidence_item`, `experience_fact`) and the broad `EntityRefType` retires, so model, §12 validator, and §15.8 producer expose one identical closed target domain. Rejected: keeping `EntityRefType` and rejecting upper-layer members in prose — model and storage would keep advertising values no legal producer can emit.
- 2026-07-12 — Issue #36: `ResumeBranch.job_description_id` becomes required and derives a NOT NULL foreign key to retained `job_descriptions`, matching the snapshot half of the anchor pair, and the optional-field workaround prose is removed. Rejected: keeping it optional with a producer-only copy rule — a V1 branch cannot legitimately outlive the job description that defined its relevance matching.
- 2026-07-12 — Issue #63: `exp2res detections generate` is the sole Stage 4 trigger while `gaps` and `contradictions` keep inspection and answer subcommands only, and a run retains the prior current generation when the validated candidate is content-equivalent over detector-authored fields, otherwise replacing both sets and superseding every upper layer in one transaction. Rejected: per-set `generate` aliases or always-replace — command names promising partial operations are an operator trap, and an unchanged rerun would needlessly invalidate every current snapshot and branch.
- 2026-07-12 — PR #66 review: Stage 4 retention requires an all-unanswered current generation — answered state never enters content-equivalence, and a generation containing an answered gap is always replaced with each replacement gap starting open. Rejected: letting answered state survive retention — an answer captured but not yet extracted would freeze `answered = true` on a gap the detector still derives from inputs that include it.
- 2026-07-12 — PR #66 review: §11.9's continuing-conflict replacement-row rule is scoped to replacing regenerations, deferring the retain-or-replace choice to §13.4 as the single lifecycle authority. Rejected: keeping the unconditional replacement-row sentence — a no-op rerun would have two normative outcomes, splitting row identity and history across implementations.
- 2026-07-12 — PR #66 review r2: "sole Stage 4 trigger" scopes to the sole direct detection-generation command, since §14.12 lifecycle recomputation invokes the same stage under §13.4/§13.13. Rejected: leaving "sole trigger" unscoped — an implementation could read it as forbidding the mandatory Stage 4 regeneration inside recompute.
- 2026-07-12 — PR #66 review r3: contradiction content-equivalence compares the two detection references as an unordered pair, so a swapped left/right rerun is the same conflict and must not churn current generations. Rejected: imposing a canonical side ordering on the §15.8 producer — it constrains detector output for a property only the comparison needs.
- 2026-07-12 — PR #66 review r3: §9.4 propagation caps are total — the maximum over an empty source list is `unknown`, so a sourceless signal or claim has a defined structural cap while §13.7 rule 1 still fails sourceless claims at verification. Rejected: minimum-length source lists on §11.5/§11.6 — a model change that would forbid the counter-evidence-only signal shapes the models deliberately allow.
- 2026-07-12 — PR #66 review r4: Stage 4 retention is scoped to its own stage — it supersedes nothing and a direct §14.7 invocation invalidates nothing upstream, while inside the §14.12 flow the downstream Stage 5–7 regeneration still follows §13.13. Rejected: skipping Stages 5–7 when recompute retains detections — their retention semantics are a separate decision, not a side effect of detection equivalence.
- 2026-07-12 — PR #66 review r4: the §9.1 ontology line for Contradiction matches Stage 4's closed target domain — a detected conflict between effective source records and current facts. Rejected: adding upper-layer ref types back — it reopens the #57 producerless breadth.
- 2026-07-12 — PR #66 review r5: the §9.4 `high` fact ceiling is reachable in V1 only within one correction lineage — an imported non-`manual_claim` root corrected by the owner — because cross-lineage corroboration remains producerless. Rejected: dropping the `high` branch — it would make `high` a producerless `Confidence` member and foreclose the corrected-import case that is legal today.
- 2026-07-12 — PR #66 review r5: the §15.5 assessment-verifier input carries `source_evidence_items`, the duplicate-free evidence set reached through the supplied source facts, so Stage 7 judges claim confidence under §9.4 from supplied data rather than hidden state. Rejected: dropping the §13.7 rule 2 strength and scope judgment — it is the semantic half §9.4 explicitly assigns to Stage 7.
- 2026-07-12 — PR #66 review r6: §15.5 `source_facts` is the claim's provenance closure — its own `source_fact_ids` plus every listed source signal's supporting and counter facts — so a signal-only claim still supplies the underlying evidence. Rejected: requiring the writer to restate underlying facts on every claim — it duplicates signal provenance into claim source lists §11.6 deliberately keeps separate.
- 2026-07-12 — Issue #62: the §15.5 verifier input is exact and deterministic — duplicate-free ID-ordered source signals, the provenance closure of facts, and the evidence items and retained logs reached through it — and Stage 7 fails closed on a missing, superseded, duplicate, or extra member before any provider call. Rejected: adding current contradictions to the per-claim input — closure counter facts already carry the conflict evidence and unrelated rows would widen the declared transmission surface.
- 2026-07-12 — Issue #39: `SelfClaim.counterevidence` entries become typed `CounterevidenceItem` records — a verifier-authored statement plus a polymorphic reference typed by the new `CounterevidenceRefType` — validated against the table the type selects, required to be members of that claim's §15.5 bundle, and duplicate-free. Rejected: keeping free counterevidence prose — the one evidential assertion the mirror leans on would stay unlinked while every other cross-layer assertion is typed.
- 2026-07-12 — Issue #53: `career` and `learning` retire from `AssessmentScope` for lack of a deterministic input-selection policy or distinct V1 consumer, leaving `global` and `project`, and a scope value returns only with its selection semantics. Rejected: defining ActivityContext-based career and learning filters now — every candidate partition either duplicated `global` or hard-coded a product judgment no V1 consumer needs.
- 2026-07-12 — Issue #53: Stage 6 input selection is structural and service-owned — `global` supplies every current fact and signal, `project` supplies target-matching subject facts plus the signals referencing them and those signals' context facts, gap and contradiction sets are never scope-filtered, an empty subject fails before the provider call, writer citations must name supplied objects, and Stage 7 receives scope and target to judge scope fit (§13.7 check 11) — while replacement identity is the assessment view, keyed by scope plus, for `project`, the NFC-trimmed case-folded target persisted pre-fold. Rejected: prompt-only scope, one-current-per-scope replacement, or byte-exact target identity — they let the writer silently rescope, destroy another project's current mirror, or admit invisibly different duplicate views.
- 2026-07-12 — Issue #61: lifecycle recomputation ends at Stage 5 — Stages 3–5 are parameterless shared derivations rebuilt automatically, while Stage 6–7 views and Stage 10–11 branches are parameterized projections regenerated only by their own commands, so the invalidating transaction captures the affected views and branches and the command reports each view with an executable §14.9 command and each branch with captured context plus the §14.10 command shape. Rejected: a persisted desired-view registry — a new owner-config entity with CRUD surface and post-deletion privacy questions, to automate a loop the owner can run explicitly.
- 2026-07-12 — PR #67 r8+r10: the §15.5 bundle gains the view context — `scope_signals` and `scope_facts`, the complete deterministic §13.6 selection re-derived from current rows — so an omitted contrary member grounds a non-passing status and may persist as a typed counterevidence reference, with `CounterevidenceRefType` gaining `self_signal`. Rejected: passing every view fact's evidence and raw-log graph per claim — a raw-text transit blow-up against data minimization when fact rows already carry the conflict.
- 2026-07-12 — PR #67 r8: `ExperienceFact.project` is copied provenance (§13.3 rule 13) equal to the governing source record's value including `None`, so an authored, renamed, or re-cased value is invalid structured output. Rejected: selecting project subjects through linked raw logs at Stage 6 — it duplicates the governing-record resolution rule 10 owns and lets a fact diverge from its stored label.
- 2026-07-12 — PR #67 r9: §14.4 correction capture copies the target's `project` like its `OccurredAt`, exactly unless the owner explicitly replaces or clears it, so a text-only correction cannot silently strip the label §13.6 project views select by. Rejected: root-project inheritance at extraction — it makes clearing a mislabeled project inexpressible and splits governing-record semantics between capture and extraction.
- 2026-07-12 — Issue #37: assessment exports move to per-view directories `out/assessment/<view>/…`, where `<view>` is `global` or `project--<target>` with the case-folded canonical target percent-encoded, so simultaneously current views export side by side and removal rules target one view. Rejected: last-export-wins on fixed paths — it silently destroys the other view's export and leaves §13.7's removal rule unable to say whose files it removes.
- 2026-07-12 — Issue #60: every top-level §11 entity `id` derives to a per-table TEXT PRIMARY KEY, and service-assigned opaque collision-resistant IDs are immutable, retried locally on collision, and never reused within their table for the workspace lifetime, while imported identifiers stay provenance in `RawLog.external_ref`. Rejected: global cross-table uniqueness or counter-derived IDs — references are already table-scoped, and a privacy purge can reset a counter's basis and enable reuse against retained telemetry.
- 2026-07-12 — Issue #45: append-only `schema_meta` history is the sole schema-version authority, compatibility is established before business I/O, and only explicit `exp2res db migrate` may upgrade — after a verified WAL-inclusive backup, in one all-or-nothing validated transaction that preserves raw-record content, while §13.13 owner deletion removes managed backups or reports residuals. Rejected: `PRAGMA user_version` or automatic on-open migration — the first has no history, the second lets any command rewrite the canonical personal store as a side effect.
- 2026-07-12 — Issue #46: V1 permits many readers and exactly one workspace business writer, holding an exclusive advisory lock on `.exp2res/lock` across all business transactions and coupled `out/` work, with persistent WAL, writer `synchronous = FULL` and `BEGIN IMMEDIATE`, bounded `busy_timeout`, single-snapshot read transactions, and bounded-wait `workspace_busy` diagnostics. Rejected: database transactions alone or PID lock files — the first cannot serialize managed-output filesystem work, the second needs stale-lock heuristics after crashes.
- 2026-07-12 — Issue #49: all eight recomputable tables gain `produced_by_run_id` plus a per-swap `generation_id` never exposed at the LLM boundary, §13.13 flows gain one orchestration run with child `parent_run_id` links, `processing_runs` gains privacy-safe content-free execution identity, and `VerificationFinding` becomes a persisted append-only §11 entity carrying each completed attempt's status, reason, unsupported phrases, typed counterevidence, and advisory rewrite as inspect-only history purged by owner deletion, while failed verifier runs persist telemetry only. Rejected: keeping findings denormalized-only — re-verification history would be uninspectable, failed attempts indistinguishable, and advisory rewrites lost.
- 2026-07-12 — Issue #50: one canonical base-model policy applies `extra = forbid`, strict typing with ISO-8601-string-to-`datetime` as the sole boundary coercion, assignment validation, lifecycle-only mutation, and sorted-key whitespace-free serialization for §12.13 hashes, while entity metadata narrows to a bounded inert named-key channel and size, count, and depth limits are preflighted. Rejected: lax or coercive validation and unlimited payloads — schema-shaped values would diverge across implementations and a valid response could exhaust memory or disk.
- 2026-07-12 — Issue #64: each retained correction displaces exactly the whole record named by `corrects_log_id`, effective lineage members are computed deterministically with no LLM call, and displaced text and `manual_claim` items never re-enter extraction while non-`manual_claim` evidence owned by displaced records survives as prose-free support descriptors. Rejected: assertion-level correction resolution — V1 has no typed resolution artifact or merge semantics, so two implementations could not derive one effective set from prose.
- 2026-07-12 — PR #68 review: the prose-free displaced-record projection is universal across §15 inputs — items linked to displaced records serialize as support descriptors and displaced `RawLog` objects never enter prompts. Rejected: forbidding facts from citing displaced support at all — it reopens the §9.4 corrected-import `high` ceiling #64 deliberately preserved.
- 2026-07-12 — PR #68 review: per-call execution identity moves to the storage-only §12.15 `llm_calls` table, keyed by (`run_id`, `call_index`) with that invocation's hashes, request ID, accounting, and retry identity, while `processing_runs` keeps run-level identity as the sole production-provenance anchor. Rejected: aggregate or last-call-wins columns on the run row — lossy overwrite semantics; one run row per call — it breaks the single generation anchor of atomic swaps.
- 2026-07-12 — PR #68 review: the §13.13 owner-deletion purge sets every retained `llm_calls.input_hash` and `output_hash` to NULL, keeping only values not derived from owner content bytes. Rejected: unconditional retention or keyed hashing — deterministic hashes are a candidate-confirmation oracle for guessable purged content, and a key sharing the workspace does not remove it.
- 2026-07-12 — PR #68 review: exact-recomputation identity is the run execution configuration (provider, model, prompt-policy hash) plus call `input_hash`, and the §12.15 hash byte form is pinned to sorted keys, UTC `Z` datetimes with six fractional digits, raw UTF-8 strings with mandatory-only escapes, integer-only numbers, and SHA-256 lowercase hex. Rejected: payload-only equivalence or a per-implementation byte form — the first classifies cross-model attempts as repeats, the second falsifies §12.15's cross-implementation hash equality.
- 2026-07-12 — PR #68 review: `GapQuestion.question` is bounded at 1,024 UTF-8 bytes, so §14.7's copied question metadata stays inside the 4 KiB entity-metadata budget and an oversized Stage 4 candidate is invalid structured output. Rejected: raising the metadata budget or copying a truncated excerpt — the first weakens a generic bound for one producer, the second breaks the answer's self-containment.
- 2026-07-12 — PR #68 review: every accepted `datetime` is offset-aware — an ISO 8601 string must carry `Z` or a numeric offset and a naive value fails at transport, hydration, and construction — so the §11 hash rule's UTC normalization is total. Rejected: interpreting naive values as UTC or in a workspace timezone — the first leaves ambiguous stored semantics, the second makes identical bytes hash differently across environments.
- 2026-07-12 — PR #68 review: `OccurredAt` bounds are offset-aware at every precision, and §11.1 states that coarse-precision time-of-day is representational with `precision` alone carrying the temporal meaning. Rejected: a naive date-only exception or retyping coarse bounds as `date` — the first reintroduces undefined UTC normalization, the second ripples a second temporal shape through storage, serialization, and the pinned hash form.
- 2026-07-12 — PR #68 review: resume-branch replacement identity is the NFC case-folded branch name — fold-equal generations supersede, selectors resolve by folded identity, and two current branches never fold equal — with the canonical check in the Stage 10 transaction under the writer lock and the raw-name partial unique index as backstop. Rejected: a stored service-folded name column — duplicated identity state SQLite cannot re-verify and that can drift from `name`.
- 2026-07-12 — PR #68 review: every fact's selections include at least one effective-record evidence item — a displaced-record descriptor is additional scoped support, never the sole source — enforced as invalid structured output and atomically at Stage 3 commit. Rejected: keeping the prompt-contract prohibition without a structural check — an only-descriptor fact would persist undetected and verify against no stated content.
- 2026-07-12 — PR #68 review: the §13.3 governing record is per fact — the latest by (`recorded_at`, ID) of the effective records that fact selects — so sibling corrections govern only the facts selecting them, checked deterministically at commit. Rejected: the lineage-wide latest-effective rule — it leaks one sibling's placement and project onto facts stated only by the other, misclassifying project views and temporal provenance.
- 2026-07-13 — PR #68 review: the displaced-record support descriptor excludes `title` and serializes exactly `id`, `raw_log_id`, `strength`, `uri`, and `path`, because §11.3 leaves `title` free text that can carry source-derived prose back into §15 prompts. Rejected: normatively restricting `title` to a non-content label — it constrains every future producer for one projection's benefit and still needs migrating already-captured titles.
- 2026-07-13 — PR #68 review: §15 inputs are §11 shapes exactly as each receiving contract declares them — complete persisted shapes or declared narrower projections — and a declared projection may never be widened toward the complete entity. Rejected: passing complete entities everywhere — vacancy raw text would transit resume calls for no contract need, widening §29.3's disclosed surface.
- 2026-07-13 — PR #68 review: the §15.9 parser input carries no `JobDescription.id`, since the input is the owner-supplied vacancy payload and no entity exists until the atomic post-parse persist, so §29.3's parser row narrows to vacancy raw text. Rejected: pre-allocating the entity ID before the call — a stage-specific exception to §11's post-response rule for a field the parser never uses.
- 2026-07-14 — Issue #29: the repository license is MIT for ecosystem consistency, and the public README states the design-specification stage plainly while deferring the security policy to the selfos umbrella repository by link until code lands, with no SECURITY.md at the spec stage. Rejected: CC-BY-4.0 as a spec-only license — the repository will not stay spec-only and a later relicense of mixed contributions is avoidable friction.
- 2026-07-14 — Issue #55: every non-`init` command uses nearest-parent workspace discovery with a no-fallback `--workspace` override, destructive or cost-bearing commands require `--yes` under non-TTY stdin, `--json` stdout carries only one versioned closed result envelope, and a stable exit-code taxonomy separates completed `blocked` verdicts from operational failures. Rejected: current-directory-only discovery and free-form result text — the first breaks subdirectory ergonomics, the second denies scripts and selfos a stable machine contract.
- 2026-07-14 — Issue #56: datetimes stay offset-aware with the supplied offset preserved and every comparison on the UTC instant, the workspace-configured IANA timezone rather than ambient OS state resolves naive input and retrospective anchors with DST gaps and folds failing closed, calendar anchors use ISO Monday-start weeks and January–March Q1, V1 generated output is English over byte-exact source voice with Russian-source-to-English-facts evaluation mandatory, Unicode identity is NFC plus case folding at named points, and paths are POSIX. Rejected: UTC-normalizing storage or an ambient timezone — the first loses the source's original offset, the second lets one workspace persist different instants on different machines.
- 2026-07-14 — Issue #51: transport failure, §15.1 validation failure, and valid negative results are normatively separate — transport retry is bounded, foreground-only, and deadline-limited, context overflow fails closed with no partition or truncation, multi-call stages persist no partial result and rerun whole, cancellation leaves the prior current generation or §13.13 invalidated state, and budgets are config-owned with privacy-safe cost reporting. Rejected: deterministic partition/merge or truncate-to-fit — the first is a large correctness surface V1 does not need, the second silently violates completeness and provenance invariants.
- 2026-07-14 — Issue #48: `jd list`, `jd show`, and `jd delete` land with a privacy-first in-transaction purge of dependent branches, bullets, findings, and managed outputs — never foreign-key-blocked and with no recompute — alongside `workspace purge`, which removes every managed content class including telemetry and backups while retaining secret-free configuration. Rejected: foreign-key-blocking JD deletion until branches are removed — a privacy operation must not be blockable by derived state.
- 2026-07-14 — Issue #48: erasure posture requires `secure_delete` on every connection, WAL checkpointing after each destructive flow, and `VACUUM` on workspace purge, while credentials are environment or keyring references only and literal values in config fail closed. Rejected: plaintext keys in `config.toml` as an accepted risk — it contradicts §29.4's transport-only rule and makes the workspace a secret store.
- 2026-07-15 — Issue #52: integration-envelope identity is (`source_system`, `source_record_id`) with content-hash idempotency, batches are all-or-nothing within §11's payload limit and safely retried by rerunning the file, and external contract versioning stays distinct from §12.14 database migration. Rejected: per-record partial batches or last-write-wins on changed upstream content — the first leaves the ambiguous partial state §13 forbids, the second rewrites evidence history against §5.3.
- 2026-07-15 — Issue #33: GitHub import identity is `repo@full-40-hex-SHA` under the §19.4 envelope, author and committer identity and timestamps are typed payload fields, and owner attribution is an explicit conservative field defaulting to `unknown`, with only `owner` yielding `commit_or_pr` strength. Rejected: inferring attribution from git author email matching — an unverifiable identity join and exactly the silent promotion §5.10 forbids.
- 2026-07-15 — Issue #28: direct ephemeris intake is confined to source-tagged activity-domain evidence with the selfos-side adapter owning wire format, Atlas snapshots import as `RawLog` plus `EvidenceItem` at `knowledge_state_snapshot` strength and never as ready-made signals or claims (`atlas_artifact_ref` retires for `atlas_snapshot`), §30 defers loopback-URL-only mirror and JD views, and a `private` `RawLog` marker is reserved but unimplemented. Rejected: direct learning-event knowledge imports or ready-made signal imports — knowledge must route through Atlas and §5.10 requires explicit promotion inside Exp2Res.
- 2026-07-15 — Issue #47: managed-output directories derive only from opaque service-assigned snapshot and branch IDs, with closed manifests carrying view identity, titles, job-description identity, and a canonical render-input hash; this supersedes #37's name-keyed scheme and retires percent-encoded slugs and the `assessment` name reservation. Rejected: keeping slug paths with hardening — every encoding, reservation, and fold-collision rule fought the root cause that user text was a path.
- 2026-07-15 — Issue #47: managed-output publication writes the complete member set to a same-filesystem temp sibling with the manifest last, makes one final atomic rename or exchange the publication point, applies canonical-root containment and no-follow checks to every managed operation, and lets the next writer deterministically reconcile abandoned candidates without their failure blocking a privacy deletion. Rejected: in-place overwrite or lockfile-only discipline — a crash mid-set leaves a current-looking partial export, and a lockfile does not survive a crash.
- 2026-07-15 — Issue #43 cluster 1: the §13 header's stale-export invalidation rule is the single normative home of managed-export staleness with a closed three-trigger list — supersession, verifier-state change, gap answer — while stage sites keep only trigger and affected sets and publication replacement consolidates into §13.14 rule 5. Rejected: leaving per-site mechanics — five copies of enumerate/remove/report drift independently, and the header's trigger list was already incomplete.
- 2026-07-15 — Issue #43 cluster 2: §11's preamble is the sole semantic home of the supersession lifecycle and its per-entity replacement identities, with §9.1 reference-only over it and §12 rule 9 and the §13 header left as the enforcement statements they already are. Rejected: also trimming the §12/§13 layer statements — they are deliberate query- and stage-level enforcement, not paraphrase.
- 2026-07-15 — Issue #43 cluster 3: §5 returns to the philosophy layer — §5.3 cites §13.13's purge and rebuild lifecycle instead of restating it and drops machine codes from principle prose, §5.9 and §11.9 defer the contradiction lifecycle to §13.4, and §25 gains a §27-style reference-only charter. Rejected: keeping operational detail in the principle layer — principle-side copies must track every lifecycle refinement or silently mislead.
- 2026-07-15 — Issue #43 cluster 5a: §11.14 is the single normative home of the verification-finding and advisory-rewrite inspect-only lifecycle — persisted only in finding history, never applied, never a later prompt input, never exported — and the six restating sites keep only their site-specific halves plus pointers. Rejected: a stage-rule canonical home in §13.7/§13.11 — the rule is a field-lifecycle property of the persisted entity, and two stage homes would still be two homes.
- 2026-07-15 — Issue #43 clusters 5b/5d: §9.4 remains the sole calibration home with §15.2/§15.3 citing its ceilings and caps instead of restating them, §11.7's answered-gap paragraph defers to §14.7/§17, and the known-gap-assertion rule stays canonical at §13.6. Rejected: keeping cap parameters inside the §15 contracts as self-contained prompt text — duplicated parameters are exactly the multi-home drift #43 targets.
- 2026-07-15 — Issue #54: V1's product-facing export artifact is a verified bullet pack with closed versioned companion schemas and deterministic byte-identical rendering, the full resume document model is deferred to a post-mirror iteration, and cross-bullet coherence uses deterministic planning only. Rejected: "resume export" naming for a bullet artifact or an LLM coherence pass — the first claims a document the product does not produce, the second reintroduces the cross-bullet fabrication surface §15.6's isolation prevents.
- 2026-07-15 — SDD version 0.3, status "Draft / implementation-oriented" → "Implementation-ready": #59's definition of ready holds, with gates A–C landed via PRs #66–#68 and #83–#85, the #43 dedupe pass via PR #86, and the Decision Log, §21, and §24 meta-gates verified; the version marks readiness, not freeze. Rejected: bumping to 1.0 — the spec is implementation-ready, not implementation-proven.
- 2026-07-15 — Issue #80: vendored the selfos-skills SDD-conventions template v1.0.0 as a generated AGENTS.md block and the Decision Log lint 1.1.0 into `scripts/`, with one repository-owned aggregate check in offline CI and a 2026-07-15 grandfather baseline for pre-adoption entries. Rejected: referencing selfos-skills at runtime — a fresh offline checkout must not depend on another repository or the network for its own correctness rules.
- 2026-07-15 — Issue #72: §15.11 is the normative field-ownership matrix for every §15 transport field, and deterministic model echoes leave the output shapes — Stage 3 copies fact `project` and derives `source_log_ids`, Stage 6 service-copies the snapshot summary and gap IDs, Stage 10 derives bullet source links — with retained apparent echoes documenting their rationale. Rejected: keeping deterministic echoes as an independent cross-check — equality against a service-derivable value validates no model reasoning.
- 2026-07-15 — Issue #94: Stage 6 derives `AssessmentSnapshot.title` deterministically from the persisted (`scope`, pre-fold `scope_target`) pair as a renderer-owned literal, so the required §11.7 field has exactly one producer and no unverified prose channel opens beside `summary`. Rejected: a model-authored §15.4 title — it reopens the unverified snapshot-prose escape hatch §13.6 closes.
- 2026-07-16 — PR #100 (Codex review): §26.1's frozen "no code has shipped" README mandate becomes stage-tracking — state the current §22 phase and promise no more than the SDD specifies or merged code delivers — and the security-policy deferral now holds "until a release ships". Rejected: amending the literal every phase — a recurring drift chore.
- 2026-07-16 — Issue #69: the service owns an isolated native-schema runner with CLI-provided rules isolation, wrapper-provided bwrap read confinement, a fail-closed two-half preflight, and the effectiveness canary as source of truth. Rejected: fenced JSON parsed from agent prose plus `-s read-only` in the repository — it duplicates native schema output and leaves ambient reads open.
- 2026-07-16 — Issue #71: evaluation uses six layered pytest markers (`unit`, `lifecycle`, `contract`, `invariant`, `golden`, `live`), live is double-opt-in via `-m live` plus `EXP2RES_LIVE_LLM=1`, and `scripts/check.py` enforces a three-state §21 map with owning phases. Rejected: prompt-only eval descriptions or live models in required CI — the first lacks executable proof, the second is costly and nondeterministic.
- 2026-07-16 — Issue #69: the landed isolated runner becomes the versioned §15.12 agent-runner protocol binding every agent-backed adapter — contract workspace, canary-verified read confinement, ambient isolation, ephemeral execution, schema-constrained final output, fail-closed preflight — named identically across §15.1, §21.50, §22, §24.53, and §29. Rejected: a Codex-only contract — the #102 Agent SDK adapter would need a second drifting copy of the same boundary.
- 2026-07-16 — Issue #103: §29.2 admits an externally-managed CLI session as the second §15.10 rule 4 credential form — no in-config slot, the external runtime owns authentication and refresh, and a missing or unauthenticated session fails outward calls closed. Rejected: an API-key slot on every adapter — it forces extracting subscription OAuth tokens into environment variables, strictly worse secret hygiene.
- 2026-07-16 — Issue #102 ratified as §15.13: a closed V1 lineup — default agent-backed `codex-cli` on `gpt-5.6-sol` written by init, secondary `claude-agent-sdk` on `claude-opus-4-8`, required non-agent-backed `openai-compat` with configurable `base_url` — switched by `[llm]` config alone. Rejected: a LiteLLM-style multi-provider layer — §15.10 rule 10's stable failure-code mapping requires owning the transport.
- 2026-07-16 — §24.54 (from #102): config-only switching among the three §15.13 adapters becomes acceptance canon, enforcing #102's "required in V1" at the §24 gate. Rejected: leaving enforcement in issue text — #88 closes V1 against §24, not against issues.
- 2026-07-16 — Issue #109: #88's V1 gate gets an auditable acceptance ledger (`tests/acceptance_ledger.toml` plus checker) mapping each §24 criterion to phases, evals, and issues with anti-overstatement gates, and the §21 checker proves `covered` rows execute in required CI. Rejected: tracking §24 state in issue text — the #88 close would rest on archaeology, not a checked artifact.
- 2026-07-16 — Issue #114: the 50 §21 case bodies move verbatim by deterministic byte-verified script extraction into the authored canon artifact `spec/21-evals-cases.toml`, §21 keeps its stable headings and binding rule, `scripts/check_eval_cases.py` enforces the sync offline, and a § point read now spans its linked canon artifacts. Rejected: hand or LLM transcription — unverifiable copying where a script proves byte identity.
- 2026-07-16 — Issue #74: `project` stays the untransformed owner label while a service-computed `project_key` (NFC, trim, locale-independent case folding) is persisted on `raw_logs` and `experience_facts` and consumed by §13.6 selection; the key is storage-level with no §11 field and no LLM transit, and a label canonicalizing to blank fails validation at every boundary. Rejected: normalize-and-overwrite the stored label — it loses exact source provenance.
- 2026-07-19 — Issue #70: Stage 4 lifecycle identity is a deterministic service-computed structural key — gap (`target_type`, `target_id`, `reason`, `priority`), contradiction the unordered reference pair — with detector prose as payload, so equal key sets over an all-unanswered generation retain current rows and differing sets replace both sets atomically. Rejected: byte equality over model prose — harmless paraphrase would become a truth-model change invalidating every upper layer.
- 2026-07-20 — Issue #73: claim ownership moves to the claim side — a required `SelfClaim.snapshot_id` foreign key replaces `AssessmentSnapshot.self_claim_ids`, with one owner per claim enforced by schema and the Stage 6 swap — and the service-assigned claim `id` is the ordering field, so no `position` column exists. Rejected: a reverse JSON list plus procedural checks — correctness would depend on every writer repeating the same scan.
- 2026-07-20 — Issue #97: a claim whose closure resolves no §16.1 direct evidence chain, including a sourceless claim, may only receive `rejected` or `unsupported`, enforced at structured-output validation, while the pass still completes with a durable finding. Rejected: failing the whole run — an opaque failure instead of a mirror-visible finding.
- 2026-07-20 — Issue #97: `assess verify` exits code 10 "blocked" iff the committed snapshot aggregate fails §16.11's assessment-export allowlist, while `contradicted` and `needs_clarification` exit 0 and code 10 keeps the completed run row and complete findings. Rejected: blocking on any non-`supported` finding — labeled-weakness statuses pass assessment export, so that predicate misreports the mirror's own gate.
- 2026-07-21 — Issue #96: `logs show --log-id` joins §14.14 rule 7's inspection surface with a `raw_text`-free JSON projection plus complete linked evidence-item projections, while human mode alone additionally prints the selected record's own `raw_text` under one narrow single-record carve-out. Rejected: a `raw_text`-free bundle in both modes — owners and agents diagnosing a system under construction need the record text without opening the database by hand.
- 2026-07-25 — Issue #158: V1 ships no per-record `private` marker, and §29.2 becomes the standing position that processing confidentiality has two controls — a self-hosted `openai-compat` endpoint, or a provider trusted with every §29.3 class. Rejected: shipping the absolute-deny marker now — real secrecy needs a local model the owner cannot yet afford.
- 2026-07-25 — Issue #159: owner captures may persist authorized inert artifact locators as ordered `artifact_reference` evidence, with the manual claim first and §9.4 unchanged. Rejected: treating an owner-typed locator as an independent source granting one owner record `high` — locator and prose are one unverified assertion, and source independence prevents repetition from becoming corroboration.
- 2026-07-25 — Issue #161: non-prompt daily, retrospective, and gap-answer capture requires an explicit command-local `--owner-authored`, and bounded UTF-8 file or stdin text is classified like prompted capture, with stdin carrying no path. Rejected: keeping the TTY gate as the authorship guarantee — terminal shape proves neither authorship nor safe multiline intake.
- 2026-07-25 — Issue #163: retrospective capture asks precision first, omits the period for `unknown`, rejects a supplied period in that state, and requires the matching period shape for every other precision. Rejected: requiring a placeholder period and discarding it — invented input contradicts uncertainty preservation and silently loses an owner-supplied value.
- 2026-07-25 — Issue #165: the shipped per-invocation deadline is 600.0 seconds at default `high` effort, covering the documented 130-second live upper bound in both init and built-in defaults. Rejected: lowering the default reasoning effort to `medium` — it weakens the selected model's evidence-sensitive work instead of sizing transport for measured behavior.
- 2026-07-25 — PR #169 review: §29.4's selected root for `privacy.ignore_paths` is the canonical workspace root at every boundary — a pattern carrying a separator anchors there, one without matches any canonical component at any depth. Rejected: anchoring to the invocation directory — the later re-check runs from an arbitrary directory, silently dropping a rule that held at capture.
- 2026-07-25 — PR #169 review: `privacy.ignore_paths` is matched by gitignore rules rather than an `fnmatch` approximation — `**` spans zero or more directories, every other wildcard stops at a separator, and an ancestor match covers everything beneath. Rejected: keeping `fnmatch` — its separator-crossing `*` over-ignores while `**/` under-ignores, so declared syntax and enforced gate disagree in both directions.
- 2026-07-26 — Issue #160: `OccurredAt` records ongoing work as an open-ended `date_range`/`approximate_range` with a null `end`, typed explicitly at capture as `start/..` and stored in the existing nullable column. Rejected: an `ongoing` marker field — a second representation of one truth that every §15 transport must then police.
- 2026-07-26 — Issue #160: §16.7 gives an open-ended placement unbounded comparison width and, as a containing interval, the attested window `[start, recorded_at)` of its governing record, while as a contained interval it stays `[start, ∞)` and entails no bounded candidate. Rejected: `[start, ∞)` in both roles — containment above the start becomes vacuous, leaving only model judgment.
- 2026-07-26 — Issue #160: §17 renders an open period as its stored start, an explicit open-period label, and a labeled as-of anchor from the attesting `recorded_at`, never an end bound or wall-clock value. Rejected: "to the present" — it reads as a verified claim about read time and ages into a false one.
- 2026-07-26 — Issue #174 corrects the 2026-07-25 #159 entry: §9.4 did change — the `high` ceiling gained route (b), an owner-captured root whose owner-typed locator survives an owner correction — and that trust is intended, since §16 and Stage 7 gate content independently of confidence. Rejected: narrowing route (b) to service-checkable locators — it caps an owner behind a closed perimeter below one with public URLs.
- 2026-07-26 — Issue #171: `RawLog.external_ref` and `import file`'s `EvidenceItem.path` persist the authorized canonical real path rather than the supplied spelling, so §29.4's pre-serialization re-check needs no resolution root. Rejected: re-resolving a stored relative spelling against the workspace root — the later check would authorize a different file than capture read.
- 2026-07-29 — Issue #152: §17's escaping becomes one closed positional set — character references for `<`, `&`, tabs, and boundary spaces; backslash escapes everywhere for `\`, backtick, `*`, `[`, `]`, `~`, and non-intraword `_`; the block markers only at the line-leading position — so determinism rests on the rule's totality and ASCII-only positionality while `report.md` stays legible. Rejected: keeping the blanket ASCII-punctuation set — it leaked backslashes into viewers that unescape less than CommonMark.
- 2026-07-29 — Issues #156/#157/#164: human mode renders the typed content the envelope already carries — `facts show` every §11.4 field but `metadata`, `signals generate` its complete produced set, and every command its §13.13 rule 9 view reports with regeneration commands, including on the failure path — with envelope version 1 unchanged. Rejected: a typed `signals generate` result — it widens the v1 envelope while the reported gap is that human mode shows nothing.
- 2026-07-29 — Issue #153: §17 merges Unknowns and Questions Worth Answering into one "Unknowns and Open Questions" section where each referenced gap renders its question text first, then ID, target, reason, and priority in gap-ID order, so every question sits beside the identity `gaps answer` needs. Rejected: cross-referencing two sections by shared IDs — the reader still joins two lists by hand, which is the reported defect.
- 2026-07-29 — Issue #154: `report.md` stops repeating the §13.12 typed closure — the Evidence Map section is removed, each rendered claim block ends with one renderer-owned sources line, and Strongly Supported Facts rows show their `source_log_ids` — while `evidence_map.json` remains the complete machine-checkable closure. Rejected: keeping the four-class ID dump in the report — a third of the document at the smallest realistic corpus, growing linearly with it.
- 2026-07-29 — Issue #180: `correction add` gains §14.2's non-prompt form (`--file <path>` or `--file -` under `--owner-authored`) and turns §14.4's copy-unless-replaced mechanics into explicit `--precision`, `--period`, `--confidence`, `--project`, and `--clear-project` flags that are invalid usage without `--file`. Rejected: `--file` copying placement and project silently — a whole-record restatement is exactly where a silent copy is least safe.
- 2026-07-29 — Issue #150: each externally-managed-session adapter declares one optional `[llm]` key naming its local executable as an absolute POSIX path, resolved strictly at capability time and never a credential slot. Rejected: leaving `PATH` as the only lever — the owner would then have no configured remedy and would see a provider outage.
- 2026-07-29 — Issue #151: §15.10 rule 10 gains `transport_request_rejected` and `transport_model_unavailable` — non-retryable classes named by owner remedy, not provider message — and rule 9 lets human mode name the failing stage and contract. Rejected: surfacing the provider's error channel behind redaction — §12.15's content prohibition is worth more than the detail.
- 2026-07-29 — Issue #155: new §16.14 fixes the referring form in owner-referential generated voice as second person or subject-free, forbidding third-person owner pronouns, role nouns, and the owner's personal name; §15.1 rule 10 and §15.4 pin it, §13.7 check 12 makes a violating claim `rejected`, and V1 enforcement stays semantic. Rejected: uniform third person — a mirror that narrates a case study contradicts §2/§3's orientation framing.
- 2026-07-29 — Issue #155 (PR #188 review): §16.14 binds verification runs, not stored history — a pre-adoption verdict keeps its run's recorded status until the next §13.7 re-verification applies current rules. Rejected: an upgrade gate invalidating derived state over phrasing form — whole-workspace churn for no truth change.
- 2026-07-29 — Issue #155 (PR #188 review): §16.14 names first-person owner voice as out of form and generalizes the Stage 7 `rejected` trigger to any out-of-form owner reference, with §15 instruction pins covering every authored natural-language field including warning messages. Rejected: keeping the trigger third-person-only — the enumeration gap lets "I …" candidates through Stage 7.
- 2026-07-29 — Issue #162: the §17 mirror becomes two members of one managed set — `report.md` plus a self-contained inert `report.html` rendered from the same section model in one export transaction, never a Markdown conversion — carrying one inline stylesheet admitted by its own SHA-256 hash under `default-src 'none'` and no script, form, or URL. Rejected: converting `report.md` with a Markdown library — it adds a dependency whose version changes the bytes §13.14 pins.
- 2026-07-29 — Issue #162: adding a fixed member bumps the managed manifest to `manifest_version = 2`, and a set published under a superseded version is never matching, current, or overwritten in place — publication aborts and reports its path as an owner-removed residual. Rejected: keeping version 1 — a conforming implementation already emitted it, and the member set is part of the rendering contract.
- 2026-07-29 — Issue #177: §16.13 gains source-script fidelity — generated prose reproduces source-named entities in the source script under NFC, and transliteration is out of form — plus a deterministic §15.1 tripwire failing a mixed Latin/Cyrillic token not NFC-equal to a complete token in the call's serialized input, with stored content never evaluated. Rejected: a semantic-only instruction fix — the mutant class survives instruction pressure, so the gate belongs at validation.
- 2026-07-29 — Issue #178: §17 re-keys section rule 5 from `claim_kind = "pattern_signal"` to the four orientation dimensions and renames the section Recurring Patterns and Interests, so selection keys on what a claim characterizes and its §16.11 status rather than synthesis form, with §15.4 pinning dimension as a per-claim content decision. Rejected: a deterministic claim-kind distribution gate — it pressures the writer to manufacture kind variety instead of fixing the routing key.
- 2026-07-29 — Issue #178 (PR #208 review): the §17 re-key and section rename change member bytes for an unchanged render-input bundle, so the assessment manifest bumps to `manifest_version = 3` and any version-2 set is superseded rather than matching or current. Rejected: keeping version 2 — an existing set would count as current under a different rendering contract, bypassing §13.14's fail-closed handling.
- 2026-07-29 — Issue #178 (PR #208 review): dimension-to-content consistency becomes §13.7 check 13 — a claim whose `dimension` mis-categorizes what it asserts under §15.4 is `rejected` — pinned in the §15.5 verifier instructions and eval §21.56. Rejected: a deterministic dimension gate — whether a claim asserts a capability or an orientation is a semantic judgment no typed field encodes.
- 2026-07-29 — Issue #178 (PR #208 review round 2): §13.7 check 13 judges non-summary claims only, since §17 routes the unique `narrative_summary` by kind alone, and it shares §16.14's stored-history semantics. Rejected: defining mixed-summary dimension semantics — a routing value §17 never reads has no correct member to define.
- 2026-07-29 — Issue #179: Stage 7 verdicts never retire, supersede, resolve, or annotate a Stage 4 detection — the remedy is evidence-driven regeneration — while §17 renders the coexistence window with counterevidence inline in each claim's block, a per-block claim-ID line, and a fixed "unadjudicated detector output" origin label, and §15.4 bars restating contradictions as claims. Rejected: Stage 7 retiring detections — polarity and ownership between a claim verdict and a detection are not computable from typed references.
- 2026-07-29 — Issue #179: the inline-counterevidence, claim-ID, and origin-label changes alter member bytes for an unchanged render-input bundle, so the assessment manifest bumps to `manifest_version = 4`. Rejected: batching the bump into a later release — §13.14 binds the version to the rendering contract, not to release cadence.
- 2026-07-29 — Issue #179 (PR #209 review): the contradiction-restatement ban gets an enforcement boundary — Stage 7's §15.5 input gains `contradictions` and §13.7 check 14 makes a restating claim `rejected`, bumping the verifier contract's schema revision. Rejected: instruction-only enforcement — the same boundary gap check 13 just closed.
- 2026-07-29 — Issue #192: SDD map lines get a routing budget — a §'s role, its distinguishing terms, and any owned canon artifact name, at most 250 characters — and change only when that routing content goes stale, narrowing the 2026-07-04 same-commit map-line rule. Rejected: full-coverage map lines — the map grew into a 2263-word compressed replica of the spec imported into every session, defeating point reads.
- 2026-07-29 — Issue #192: rule ordinals inside a §'s numbered lists are stable cited anchors exactly like § numbers — never renumbered, merged, split, mid-inserted, reused after retirement, or reassigned in meaning — and a new rule appends at the end of its list. Rejected: renumber-on-edit with a citation sweep — ordinals are cited 384× with no checker guarding them, so one missed sweep silently retargets citations.
- 2026-07-29 — Issue #98: §30's mirror and gap-question views become V1 scope behind the new §14.17 `view serve` — a literal-loopback bind answering only its own bound authority, a closed `/mirror` and `/questions` route set whose selector is a view identity or one exact snapshot ID with no default, per-request §13.14 revalidation serving exactly the revalidated `report.html` bytes, and rule 7's closed outcome table. Rejected: an implicit latest snapshot — it silently renders superseded state.
- 2026-07-29 — Issue #98: §30 rule 5's question-of-the-day handoff moves from the shell reading `self_claims.json` to Exp2Res serving that projection — revalidating current output, validating the closed companion schema, and presenting only the `question` values of unanswered `unknowns`. Rejected: keeping the shell-side read — it couples the shell to an internal companion schema and lets it bypass revalidation.
- 2026-07-29 — Issue #98: a refused view bind stays inside the closed §14.14 taxonomy as exit class 2 with `view_bind_not_loopback`, `view_bind_invalid`, or `view_bind_failed`, and `--host`/`--port` are flags with built-in defaults and no configuration or environment representation. Rejected: `[views]` configuration keys — §29.2's config carries no serving surface, and the embedding URL is configured outside Exp2Res.
- 2026-07-29 — Issue #98 (PR #211 review): loopback binding is §30's whole trust boundary — no local peer is authenticated, so while `view serve` runs any local account reaching the port reads the same two projections the owner does — recorded as a §29.6 stated limit bounded by the command's lifetime. Rejected: a capability token in the embedded URL — a stored serving credential §29.2's config has no slot for.
- 2026-07-29 — Issue #98 (PR #211 review): §30's outcome table becomes ordered rather than merely closed — authority, method, route, and selector before any state read, then §12.14 compatibility, resolution, the §16.11 gate, and revalidation last — and rule 2 names `GET` and `HEAD` as the accepted methods, `HEAD` emitting the same status and headers without a body. Rejected: a per-row precedence note — precedence is one ordering, not nine.
- 2026-07-29 — Issue #98 (PR #211 review r1): §30 rule 6 narrows to what it was for — beyond the two projections a response carries only rule 7's outcome vocabulary and renderer-owned literals, and a diagnostic only the outcome class and closed route — and every response declares `Cache-Control: no-store`. Rejected: a validator-based cache with `ETag` — the served bytes are already revalidated per request, and a conditional round trip adds a second currentness authority.
- 2026-07-29 — Issue #98 (PR #211 review r2): §30 rule 7's table gains a stable literal class per row and splits the rows whose remedy was wrong for part of the row, while rule 6 pins `text/html; charset=utf-8` and separates the bounded SQLite reader from the workspace writer lock. Rejected: leaving classes to implementations — a closed vocabulary no test can assert is not closed.
- 2026-07-29 — Issue #98 (Codex adversarial pass): §29.6 records that §13.14's manifest proves currentness, not authorship — a process with write access to the managed directory can replace a member and its digest together, and every reader accepts it. Rejected: re-rendering the assessment from the graph on every served request to compare bytes — it makes serving a second renderer and doubles per-request cost.
- 2026-07-29 — Issue #98 (PR #211 review r3): every §30 response carries its literal outcome class in the service-owned `Exp2Res-View-Outcome` header, restated in the body only by refusal pages; the closed request-header set is named, an ambient host cookie is ignored, transport parsing heads the refusal order, a body-refusing response closes its connection, and the deferred project form is `scope=project` plus one `project` parameter. Rejected: the class in the body alone — it would break byte identity with `report.html`.
- 2026-07-29 — Issue #98 (PR #211 review r4): §30's outcome rows stop overlapping — 503 covers §8.1 contention alone, a blocked assessment names verification while it is unverified and generation once a verdict has completed, and 405 declares `Allow: GET, HEAD` — and rule 1 defines the fetch-metadata predicate it consults. Rejected: treating an unreadable workspace as contention — it tells the owner to wait for a command that will never finish.
- 2026-07-29 — Issue #98 (PR #211 review r5): §30 stops calling its V1 view slice implemented and calls it in scope — a binding contract on Phase 3 work that §24.59 and §21.57 hold at `pending` until it builds. Rejected: re-deferring the slice until the CLI ships — §30 is spec canon exactly like §14's not-yet-built `bullets` flow.
- 2026-07-29 — Issue #98 (PR #211 review r6): §30 stops consulting fetch metadata because the shell embeds the view cross-origin by URL, while the `Origin` check stays, and rule 6 pins literal structural matching over single-pass value decoding as rule 7 gains `assessment_inconsistent` and `export_residual`. Rejected: an allowlisted shell origin — rule 1 forbids consumer-specific configuration.
- 2026-07-29 — Issue #98 (PR #211 review r7): §30's `export_residual` covers every §13.14 residual a re-export cannot clear on its own, including rule 6's containment and no-follow skips, rather than only rule 5's malformed and superseded sets. Rejected: routing them to `internal_error` — a symlinked `out/` root is a state §13.14 predicts and names.
- 2026-07-29 — Issue #98 (PR #211 review r8): §30's `no-store` promise narrows to navigations that reach the network, `malformed_request` covers every transport parse failure rather than the request line alone, and §14.17 drains an accepted request to its outcome before the class-9 envelope. Rejected: claiming the next navigation is always a new request — history restoration is outside Exp2Res's control.
- 2026-07-29 — Issue #98 (PR #211 review r9): §8.1 stops claiming a serving process can never block a writer, since a request in flight is an ordinary concurrent reader, while §14.17 bounds the drain with receive and emit deadlines and `assessment_inconsistent` absorbs rule 3's duplicated-identity resolution. Rejected: leaving multiple current snapshots to `invalid_selector` — no corrected request repairs stored state.
- 2026-07-30 — Issue #98 (PR #211 review r10): §14.17 gets one absolute first-interrupt drain deadline across server and socket work, and §30 appends fixed request-line, header-byte, and field-count caps plus `question_companion_invalid`, with §21.57 covering both schema gates. Rejected: per-phase timers — they leave another unbounded edge.
- 2026-07-30 — Issue #98 (PR #211 review r11): §30 accepts only HTTP/1.1 origin-form with one `Host`, caps handling at 32 connections, and binds closed outcomes to computed outcomes and completed responses, while duplicate snapshots name no generation remedy. Rejected: Stage 6 corruption repair — generation is not workspace repair.
- 2026-07-30 — Issue #98 (PR #211 review r12): §21.57 explicitly covers the 405 `Allow` header, occupied-port `view_bind_failed` with no fallback, and hostile question escaping. Rejected: leaving these only in prose — the pending acceptance case could pass implementations that omit each binding behavior.
- 2026-07-30 — Issue #98 (PR #211 review r13): §24.59 limits cache freshness to network revisits, §30 makes method precede body and reserves `workspace_busy` for SQLite contention, and §21.57 pins every outcome and status pair and the JSON cancellation output. Rejected: treating the writer lock as view contention — readers do not acquire it.
- 2026-07-30 — Issue #98 (PR #211 review r14): §21.57 proves same-process invalidation freshness, receive-timeout slot release, both port bounds, integrity-before-status precedence, an empty served question projection, and content-free internal errors. Rejected: inferring those behaviors from happy paths — each omitted edge permits a conforming-looking cache, stalled connection, wrong refusal, or leaked framework diagnostic.
- 2026-07-30 — Issue #98 (PR #211 review r15): §14.17 starts one unresettable receive deadline at admission, while §30 pins body framing and separates transient `export_changed` from repairable residuals and §21.57 covers slow trickles and second-interrupt closure. Rejected: per-read timers — trickled bytes could hold every slot forever.
- 2026-07-30 — Issue #98 (PR #211 review r16): §30 accepts only an absent or one literal bound `Origin` and stops promising generation repairs an exact-ID URL, while §21.57 pins bind diagnostics, refusal redaction, and human-mode URL channels. Rejected: normalizing origin values — identical bytes need one outcome.
- 2026-07-30 — Issue #98 (PR #211 review r17): §13.14 gives §30's concurrent-publication read its narrow `export_changed` carve-out, §29 points to the complete provenance read closure, and §30 limits byte determinism to parsing and selector interpretation. Rejected: a hand-enumerated privacy read subset — it drifts below complete revalidation.
- 2026-07-30 — Issue #98 (PR #211 review r18): §14.17 owns an absolute ordinary processing deadline and exact global-identity startup URLs, §30 pins snapshot-ID syntax, §22 names the Phase 3 view definition of done, and §21.57 checks human exit 9. Rejected: selectorless startup URLs — both routes require explicit selection.
- 2026-07-30 — Issue #98 (PR #211 review r19): §14.17 gives processing a two-timeout budget and preserves one full SQLite wait, while §8.1 makes post-bind contention a request-level `workspace_busy` with serving continuing. Rejected: sharing one deadline with `busy_timeout` — prior parsing steals its promised wait.
- 2026-07-30 — Issue #98 (PR #211 review r20): §14.17 reserves one full timeout of post-SQLite slack inside a three-timeout processing budget, §30 pins raw header-line grammar, and §21.57 covers zero-row identity, `OPTIONS`, and startup contention. Rejected: tolerant header parsing — identical bytes need one pre-state outcome.
- 2026-07-30 — Issue #182: §13.4 retention is decided per output set — gap churn replaces only the gap set — and invalidation follows the replaced set's dependency graph, so a gap-only replacement supersedes views and branches but never signals, which take no gap input. Rejected: keeping whole-generation coupling — it priced a byte-identical Stage 5 regeneration on every gap re-enumeration.
- 2026-07-30 — Issue #182: a Stage 4 rerun whose §11 canonical input hash, provider, model, and prompt-policy hash equal the most recent completed §13.4 run's recorded first-call telemetry completes as full retention with no provider call and no `llm_calls` row. Rejected: comparing business state instead of telemetry — the detector input, not the detection output, proves the rerun changes nothing.
- 2026-07-30 — Issue #181: §15.2 makes in-scope displaced-support descriptor selection required rather than merely permitted, and §13.3 rule 14 adds a deterministic non-gating `displaced_support_unselected` envelope warning when a lineage's supplied descriptors go wholly unselected. Rejected: service-side auto-selection — topical fit is a semantic judgment over content only the model sees.
- 2026-07-30 — Issue #183: `assess repair` is §13.6's non-LLM second form that adopts each rejected or unsupported claim's latest persisted `suggested_rewrite` into a complete new `unverified` snapshot generation, re-verified only by an ordinary full Stage 7 pass. Rejected: applying rewrites without re-verification — verifier text would self-authorize.
- 2026-08-01 — Issue #194: Decision Log entries are one or two sentences — the decision, then the rejected alternative in the checker's canonical grammar — and `scripts/check.py` lints the log at an 80-word ceiling with a 40-word warning, with pre-2026-07-15 entries still grandfathered. Rejected: keeping the vendored 140/80 defaults — entries had grown to 75–233 words against a compact-format contract, so the ceiling never bound them.

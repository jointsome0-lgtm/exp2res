## Decision Log

Format: `YYYY-MM-DD — decision in one phrase; rejected alternative and why.`

- 2026-07-03 — Keep the SDD as a single file navigated via the § Index (map-as-interface);
  a physical split into a map file plus per-section files is deferred until after the
  structural dedup pass, and if done, must be a purely mechanical commit (concatenated
  section files must reproduce the original). Revisit triggers: code lands and sections
  graduate into living docs/tests; full-pass reviews start hitting context limits; parallel
  per-section editing becomes the norm; the file exceeds ~30–40K tokens despite dedup.
  Rejected alternative: splitting now — point reads already load only the needed section
  (§ Index + grep), a split does not fix the actual pain (cross-section duplication and
  drift), and splitting before dedup would migrate content about to be merged or deleted.
- 2026-07-04 — Structural dedup pass: §12 recognized as derived from §11 and replaced with
  derivation rules — with the exception of fact_sources and processing_runs, plus (found
  during the loss check) contradictions, gap_questions, job_descriptions, resume_branches,
  which also have no Pydantic counterpart and keep normative DDL; §22 compressed to a
  phase → §13-stages → definition-of-done table (commands live in §14; `exp2res logs list`
  rehomed to §14.11); anti-flattery mantras consolidated into §16 (+§5.10, §27) with
  one-line references from §4.3, §5.1, §15.4, §21.1, §25.2. Version not bumped — no
  decisions changed. Rejected alternative: leaving duplicates as-is — duplication cost is
  paid again in every polishing session and cross-copy drift risk grows.
- 2026-07-04 — Executed the deferred physical split (see 2026-07-03): `SDD.md` is now the map (§ Index + numbering rules), each top-level § lives in `spec/NN-slug.md`, decisions in `DECISION-LOG.md`; § numbering and all content unchanged — head + `spec/` files + log concatenate back to the pre-split file byte-for-byte (verified). Trigger: context root — file boundaries enforce the section-scoped reading the convention only requested (the exp2res monolith already exceeded the default 2000-line read window, silently truncating whole-file reads; split kept symmetric across SDD-stage repos). Rejected alternative: single file + convention — conventions don't bind agents, structure does.
- 2026-07-04 — Map aligned with the atlas pattern: § Index preamble gains "retired numbers are never reused" plus the registry (§12.1–§12.3, §12.5, §12.6, §12.9, §12.12 — the dedup's derivable tables); header Date field dropped (stale one day after writing — git is the date authority); henceforth a § edit updates its map line and status in the same commit. Rejected alternative: hand-maintained Date and per-§ retirement notes only — a duplicate of git metadata that had already drifted, and a registry readers can't find from the map.
- 2026-07-04 — §20 rewritten from a 112-line speculative file tree to placement principles + a normative skeleton (issue #1, the atlas §8 pattern): grep across spec/ showed no other § relies on any source path in the tree — the only load-bearing paths (out/*, .exp2res/, logs/) are runtime workspace owned by §14.1/§13.12 and were never in it, and docs/SDD.md was already falsified by the physical split. Skeleton keeps the root the repo already has plus the layer packages; module/test/example names now derive from §13, §21+§27, §19/§14 instead of being pre-named. Rejected alternative: keeping the full tree — it restates §13/§21/§27 at 112-line cost per full pass and drifts the moment code lands.
- 2026-07-04 — Issue #2: the four §9.1 ontology entities that had only DDL — Contradiction,
  GapQuestion, JobDescription, ResumeBranch — get Pydantic models (§11.9–§11.12) and §12 now
  derives their tables (§12.7, §12.8, §12.10, §12.11 retired); normative DDL remains only for
  the two non-ontology storage artifacts (fact_sources, processing_runs), making §12's
  exception list principled instead of enumerated. Grounds: they cross the LLM boundary
  (§15.3/§15.4 inputs, stage-4/8 outputs) where §15.1 mandates Pydantic validation; §11 already
  holds typed refs to them (AssessmentSnapshot.gap_question_ids/contradiction_ids,
  ResumeBullet.branch_id); and §5's "contradictions are first-class" is hollow if Contradiction
  is an entity without a domain model. Supporting changes: §10 gains EntityRefType and
  GapTrigger (now the canonical home of §13.4's trigger list); derivation rules gain
  bool→INTEGER and polymorphic-ref-without-FK; §11 preamble fixes its scope as "persisted
  ontology entities" and records why VerificationFinding stays contract-level (§15.5/§15.7,
  stored denormalized on its targets). Rejected alternative: declaring the four
  storage-level-only in §12 — saves no spec volume (models replace equal-sized DDL), leaves a
  third of the ontology without domain representation, and defers the same modeling work to
  implementation time where it would happen ad hoc and unreviewed.
- 2026-07-05 — Issue #24: §20 principle 3 points job descriptions to §13.8
  (Stage 8 — Job Description Parsing) instead of §13.11 (Resume Verification).
  Rejected alternative: leaving the pointer for readers to infer — it sends
  `examples/` population to the wrong pipeline stage.
- 2026-07-05 — Issue #6: §10 is the canonical, mechanically extractable home for every enum/Literal value list used by §11 persisted models, and §13/§27 are reference-only mirrors where they mention canonical lists or rules; §11 replaces inline Literals with named §10 aliases, §13.5/§13.6 point to the §10 alias plus §11 field instead of listing values, and §27 points to canonical §16 rule numbers with a same-commit sync rule. The shape is intentionally compatible with a post-MVP generated docs/schema/lint pass, but no generator, linter, separate registry file, runtime schema tooling, generated documentation, or MVP test infrastructure is introduced now. Version not bumped — no enum values, verifier behavior, schema fields, or export safety rules changed. Rejected alternative: leaving model-local literals or prose restatements — it preserves multiple homes for the same list and repeats the atlas drift failure; rejected alternative: implementing generated enum docs now — useful later, but premature before MVP and likely to turn an SDD dedup issue into tooling work.
- 2026-07-11 — Issue #11 evidence classification: §10 owns the ten-value `EvidenceStrength` taxonomy, `EvidenceItem.strength` is typed by it, and GitHub-commit evidence recording (now Stage 1 under issue #19) uses `commit_or_pr` for the same commit as §19.3; `EvidenceItem.evidence_type` is removed because source origin already lives on `RawLog` and the field had no distinct values or consumers. Rejected alternative: introduce a second `EvidenceType` taxonomy — it would duplicate the strength/origin axes and enlarge v1 without a demonstrated use.
- 2026-07-11 — Issue #11 fact-source relations: `fact_sources.support_type` is constrained to `direct` or `corroborating`, independently of `EvidenceStrength`, and every fact requires a direct source. Rejected alternative: leave an open string or reuse evidence-strength values — either preserves an undefined value space or collapses source relationship and source strength into one axis.
- 2026-07-11 — Issue #16 temporal shape: `OccurredAt.precision` is the sole discriminator, its value determines the legal `start`/`end` shape, and the redundant `OccurredAt.kind`, `OccurredAtKind`, and `occurred_kind` storage column are removed. Rejected alternative: persist both fields plus a legal-pair matrix — it stores derivable state and adds a consistency surface with no v1 information gain.
- 2026-07-11 — Issue #16 confidence axes: `Confidence` types general confidence on `ExperienceFact`, `SelfSignal`, and `SelfClaim`, while `TemporalConfidence` is reserved for `OccurredAt.confidence`. Rejected alternative: reuse `TemporalConfidence` because the members currently match — it conflates confidence in when something happened with confidence in what a derived claim asserts.
- 2026-07-11 — Issue #15 ownership comparison: `OwnershipLevel` is a normative total order from `unknown` through `led`, with `unknown` weakest and claim confidence kept independent; verifiers fail closed when ownership language cannot be normalized. Rejected alternative: make `unknown` incomparable — a verifier could not conservatively decide whether any known ownership phrase is an upgrade.
- 2026-07-11 — Issue #15 temporal comparison: non-range `TemporalPrecision` values compare by a weak-to-strong rank and normalized maximum uncertainty width, while ranges compare by actual width with approximate bounds weaker at equal width. Rejected alternative: compare raw enum positions — range precision depends on duration, so a fixed position would misclassify cases such as a two-day range versus a month.
- 2026-07-11 — PR #30 review: zero-width ranges are illegal — `date_range`/`approximate_range` require `end` strictly after `start`; an instant is `exact_datetime`/`exact_day`. Rejected alternative: define a precision-implied width for zero-width ranges — it would give one moment two representations and make §16.7's equal-width exactness rule ambiguous.
- 2026-07-11 — Issue #26 creation timestamps: every persisted entity model except `RawLog` carries system-assigned `created_at` at first persistence, `RawLog.recorded_at` keeps its §5.4 meaning, and §12 derives every entity timestamp without a `resume_bullets` exception. Rejected alternative: infer creation solely from `processing_runs` — §15.1 guarantees run metadata only for LLM calls and §12.13's untyped output IDs cannot identify the first persistence of every entity without a larger provenance design.
- 2026-07-11 — Issue #19 pipeline identity: a stage requires a §14 trigger and a downstream-referenceable persisted business result; evidence recording now occurs atomically in Stage 1, relevance matching occurs inside Stage 10 and persists on `ResumeBullet`, and §13.2/§13.9 are retired without renumbering, leaving ten active stages. Rejected alternative: preserve paper stages or force exactly nine by hiding an independently triggered persisted verifier/export operation — either keeps the original contradiction or violates the same stage criterion.
- 2026-07-11 — Issue #20 CLI authority: §14 is the only home of command forms, noun-led generation groups use an explicit `generate` verb, required paths stay positional, and stored-record selectors plus generation/export context use named options; §13 carries semantic trigger references only. Rejected alternative: retain per-stage command copies or bare-noun generation — both preserve grammar drift surfaces already falsified by assessment verification and contradiction commands.
- 2026-07-11 — Issue #21 entry producers: V1 keeps only the eight `EntryType` values with explicit capture/import producers, restricts Atlas to artifact references and GitHub to commits, and makes `import file` a design-document importer; six producerless or contractless values are retired and may return with contracts. Rejected alternative: add speculative note, competition, learning, Atlas-trail, PR, and issue importers — it enlarges V1 to preserve unused enum members.
- 2026-07-11 — Issue #21 raw-source boundary: `llm_inferred` is removed from `SourceType`, and LLMs may neither create nor mutate `RawLog`; inference begins at `ExperienceFact` and remains attributable to processing runs. Rejected alternative: define an LLM raw-log producer — it would place derived interpretation in the immutable user/imported truth layer.
- 2026-07-11 — Issue #21 export claims: `export_claim` is removed from `ClaimKind`; resume bullets remain a distinct projection carried by source links and verification fields, while fact and assessment contracts own the remaining claim kinds. Rejected alternative: add a constant-like `claim_kind` field to `ResumeBullet` — it adds no v1 distinction or verifier behavior.
- 2026-07-11 — Issue #22 runtime workspace: V1 initialization creates only `.exp2res/` and `out/`; SQLite owns raw/evidence records, while supplied files remain in place and their paths are stored as provenance references. Rejected alternative: copy inputs into managed `logs/` or `evidence/` directories — it adds a second file lifecycle and ambiguous source-of-truth semantics without a V1 consumer.
- 2026-07-11 — PR #31 review: the assessment verifier output (§15.5) carries `counterevidence` — contrary-evidence statements grounded in supplied sources, empty when none — so Stage 7 can persist `SelfClaim.counterevidence` through a validated payload. Rejected alternative: keep the field outside the verifier contract — no other flow produces counterevidence, leaving §11.6's field unfillable under §15.1's fail-closed rule.
- 2026-07-11 — PR #31 review: `gaps answer` sets `GapQuestion.answered = true` together with `answer_log_id` in one transaction, and `answered` is true iff `answer_log_id` is set. Rejected alternative: drop the bool as derivable from `answer_log_id` — §12 rule 4 already derives a persisted NOT NULL column from it, and removing a §11.10 model field is a model change beyond this command-note fix.
- 2026-07-11 — Issues #3 and #9 data lifecycle: raw records are immutable to automation but owner-deletable; corrections append linked records and replace one coherent current derived generation while preserving superseded snapshots as inspectable history, whereas owner deletion hard-deletes the selected raw record, globally purges all derived database generations, verifies managed-export removal with residual-path reporting, and rebuilds from surviving sources without allowing cleanup/rebuild failure to undo database deletion. Rejected alternatives: warn-and-skip or selective graph deletion — either can retain private derived text or present an incomplete model as current because V1's JSON and implicit dependencies cannot prove a selective purge complete.
- 2026-07-11 — Issue #17 provenance integrity: V1 keeps upper typed ID lists as JSON but resolves every target transactionally before persistence, while `fact_sources` uses non-null EvidenceItem links and derives raw-log provenance through `EvidenceItem.raw_log_id`, allowing multiple evidence items from one raw log; unresolved or inconsistent links fail the producing run atomically. Rejected alternatives: a generic link table cannot provide cross-table SQLite foreign keys, per-field tables add many storage artifacts without a V1 query need, and a surrogate key plus nullable evidence preserves a producerless raw-only state and null-aware uniqueness while making evidence-strength calibration ambiguous.
- 2026-07-11 — PR #34 review: `corrects_log_id` is a capture-time requirement, not a standing invariant — a correction orphaned by owner deletion (`ON DELETE SET NULL`) is a valid model state and roots its own lineage, accepted by hydration, §12 rule 10, and the §13.13 rebuild. Rejected alternatives: reclassifying the orphan's `entry_type` — a system rewrite of a raw record §5.3 forbids; purging the orphaned lineage — deletes records the owner did not select, and correction text is self-contained by construction precisely so it can stand alone.
- 2026-07-11 — PR #34 review: the §16.1 current-chain requirement binds current rows and verification/export inputs only; superseded rows are exempt inspect-only history whose references legitimately point at superseded targets, and §12 rule 9 keeps them out of all consumers. Rejected alternative: apply the chain rule to every persisted row — it would invalidate preserved history or force a purge on every correction, contradicting §11's history-retention rule.
- 2026-07-11 — PR #34 review: gap answers are self-contained at capture — `gaps answer` copies the question text and reason into the answer's `RawLog.metadata`, so surviving answers stay interpretable after Stage 4 regeneration or the §13.13 reset, and links to regenerated questions are never re-created (a resolved uncertainty no longer fires its trigger; a regenerated gap is genuinely open). Rejected alternatives: preserving `GapQuestion` rows through the reset — retains derived text the purge must remove; post-rebuild re-linking — the purged question row no longer exists to match against.
- 2026-07-11 — PR #34 review: the fact-extractor input (§15.2) carries `RawLog.metadata` through unmodified, so the §14.7 gap-answer question context reaches extraction — self-containment must hold in the pipeline, not only at rest. Rejected alternative: require owners to restate the question inside their answer text — the system already holds the context and manual restatement is unreliable.
- 2026-07-11 — Issue #8 verification semantics: §16.11 is the sole operational home for `VerificationStatus` meanings and role-aware consumer allowlists; Stage 7 reduces snapshot status over all claims including a required `narrative_summary` claim that exactly carries snapshot summary prose, assessment exports preserve visibly labeled uncertainty, and only `supported` claims and bullets may become resume content. `ExperienceFact` loses its producerless status field. Rejected alternatives: denylist gates, which let unnamed unsafe states pass, and a new fact-verifier path introduced only to justify an unused field.
- 2026-07-11 — Issue #7 resume anchoring: every `ResumeBranch` requires the exact current `AssessmentSnapshot` selected by §14.10's required `--snapshot` option, with no implicit latest default; branch, bullet, and source-claim consistency is validated before persistence and export. Rejected alternative: optional or inferred anchoring — it is ambiguous with multiple snapshots and permits the assessment and export layers to collapse.
- 2026-07-11 — Issue #18 review gates: V1 defines review as Stage 7/11 verifier gating of assessment and resume projections before their consumers, not as an owner verdict on regenerated derived rows; semantic promotions remain explicit and traceable, and producerless `SourceType.user_confirmed` is removed. Rejected alternative: durable owner confirm/dispute raw records — they require a new entry producer, verdict payload, stable matching across regenerated claim IDs, and precedence rules, enlarging V1 into a separate review subsystem.
- 2026-07-11 — Issue #23 contradiction lifecycle: V1 removes `resolved`/`dismissed` workflow state and `resolution_note` from derived contradiction rows; Stage 4 alone always replaces the complete current contradiction set, every current snapshot renders that set without scope filtering, and prior detections remain superseded history outside the owner-deletion privacy reset. Rejected alternative: resolve/dismiss commands backed by raw verdict records — regenerated contradiction IDs have no stable identity, so durable application requires semantic matching and precedence machinery, while mutating a derived row would silently lose the verdict on regeneration.
- 2026-07-11 — Issue #27 verifier mechanics: V1 verification is one validated semantic pass per current claim or bullet; §15.1 retries only invalid structured output, verifier findings and suggested rewrites are returned to the owner, and verifiers never invoke writers or mutate or drop derived prose. Revised wording requires an explicit Stage 6/10 replacement generation. Rejected alternative: an automatic writer-verifier repair loop — it requires generation orchestration, termination and gap-production rules across Stages 4, 6, 7, 10, and 11, enlarging V1 and conflicting with the superseding lifecycle.
- 2026-07-11 — Issue #10 resume generation uses one isolated §15.6 invocation per bullet with only that bullet's evidence and supported claims, typed JD requirements, exact provenance sets, and all writer-settable ResumeBullet fields. Rejected alternative: retain the historical-version pointer, batch bullet contexts, or pass snapshot title/summary prose — these leave no recoverable contract, expose cross-bullet claim inputs, or recreate the unverified wording channel closed by PR #35.
- 2026-07-11 — Issue #13 fact time inherits the governing RawLog placement, permits only explicitly evidence-backed contained narrowing, and never exceeds in-context temporal precision or governing temporal confidence; Stage 5 consumes current facts, their linked evidence items, and contradictions, with gap answers reaching it only through Stage 3 and no prior-signal input. Rejected alternative: literal source-copy-only time or direct gap-answer/existing-signal inputs — the former discards stronger supplied temporal evidence, while the latter bypasses fact provenance or mixes replacement generations.
- 2026-07-11 — Issue #14 Stage 4 semantic detection and Stage 8 JD parsing are LLM-backed under appended §15 contracts, ParsedJD owns service-assigned stable typed requirement IDs, and matching remains internal to Stage 10 with retired Stage 9 unchanged. Rejected alternative: unspecified deterministic semantics, model-authored/untyped parsed identity, or a revived matching stage — each leaves an LLM boundary unvalidated, makes service failures model-retryable, or contradicts PR #31's stage model.
- 2026-07-11 — Issue #12 assessment unknowns use the complete current unanswered Stage 4 set in existing typed AssessmentSnapshot.gap_question_ids instead of a parallel prose field, project scope_target stores the §14.9 selector without changing the AssessmentScope replacement key, and §17 renders all claim counterevidence with visible status. Rejected alternative: writer-selectable unknown strings, answered gaps, or metadata storage — these permit uncertainty omission, misrepresent answered questions, duplicate gap state, or recreate the separately unverified snapshot prose channel closed by PR #35.
- 2026-07-11 — Issue #4 verification separates generated voice from source voice: structure is validated everywhere, every untagged LLM string is generated voice, and only typed byte-verified source segments escape §16.2–§16.10 without owner/system-of-record rewriting. Rejected alternative: trusting quote-like text or scanning concatenated payloads without origin boundaries — either launders model prose as source or falsely treats user/import text as a system claim.
- 2026-07-11 — PR #38 review: snapshot gap references are synthesis-time state — `gaps answer` never supersedes or fails a current snapshot; the answered-exclusion rule binds only the Stage 6 write boundary, §17 renders a post-synthesis answer as answered-since-synthesis context, and only the next generation excludes the row. Rejected alternatives: invalidating dependent snapshots inside the answer transaction — it destroys a still-honest mirror over evidence the pipeline has not extracted yet; read-time failure on answered references — it turns encouraged owner activity into an export outage until regeneration.
- 2026-07-11 — PR #38 review: `gaps answer` applies §13's managed-output invalidation to the exports it stales — while a current snapshot references the answered question, enumerate `out/assessment/` and every dependent branch's managed set, attempt removal, report residual paths — superseding no row; snapshot and branches stay immediately re-exportable. Rejected alternative: leaving pre-answer artifacts in place — a current-looking export would keep presenting the question as open, violating the no-stale-current-output rule every other lifecycle transition obeys.
- 2026-07-11 — PR #38 review: Stage 4 consumes effective lineage evidence only — the raw logs and evidence items governing current extraction under §13.3 rule 10/§14.4, with factless effective records still targetable; content displaced by a selected correction is neither input nor target. Rejected alternative: the complete retained raw/evidence set — the detector would regenerate current contradictions and gaps from exactly the text the correction flow removed from the current fact generation, resurrecting resolved conflicts §14.4 exists to retire.
- 2026-07-11 — PR #38 review: §16.2–§16.10 are owner-referential — parsed external-demand text (§15.9) stays generated voice for structure and parse fidelity, but faithfully preserved demand wording characterizes the vacancy, cannot make `jd add` unpersistable, and is never force-rewritten; asserting the owner meets the demand stays fully bound wherever it appears. Rejected alternatives: byte-verified source-span fields on ParsedJD — §15.9's required normalization and modality preservation are not verbatim substrings; treating demand text as owner-claim voice — ordinary JDs ("expert Python") force meaning-destroying rewrites of the employer's requirement.
- 2026-07-11 — Issue #41 contract-example notation: each entity's full-shape example lives once at its producing contract (§15.2 facts/evidence/logs, §15.4 signals/gaps/claims, §15.9 ParsedJD); other examples elide repeated bodies to `<id: complete §NN Model>` placeholder strings defined in §15.1, with no normative change. Rejected alternatives: full objects in every input example — five ExperienceFact copies made each §11 field change a five-site edit and doubled §15's read weight; splitting the file — the one-§-per-file layout and the map's point-read flow are a standing convention.
- 2026-07-11 — PR #42 review: a §15.1 placeholder denotes the complete persisted §11 shape — the producer example plus exactly the service-set fields its prose names (§15.2: Stage 3's id/created_at/superseded_at; §15.9: Stage 8's JobDescription.id/created_at and JDRequirement.id) — because producer examples are deliberately model-emitted subsets. Rejected alternative: restoring a full persisted example per consumer input — recreates the duplication #41 removed while §11 already carries the normative shape.
- 2026-07-11 — PR #42 review r2: §15.1 producer references corrected (signal → §15.3, gap/contradiction → §15.8; persisted-row example sites named explicitly), and a behavior-bearing example object — one whose concrete content the same example's output depends on — is never elided, so §15.8 shows its fact and raw log in full. Rejected alternative: placeholder field-overrides for load-bearing content — inline deltas recreate exactly the example divergence the notation exists to prevent.
- 2026-07-11 — Issue #5 security/privacy boundary: the owner-controlled local workspace is Exp2Res's only canonical persistence domain; only foreground user-started runs may transmit the exhaustive typed §15.2–§15.9 payloads to an explicitly selected provider, while all other LLM/network calls, outbound telemetry/sync/push, secret or ignored context, and instruction-following from source data are forbidden; §14.5 therefore consumes a local GitHub payload, §21.30 tests JD injection, provider retention and false-but-structured source poisoning remain explicit residual risks, and any weakening requires another Decision Log entry. Rejected alternative: rely on the "local-first" header, provider abstraction, and network-shaped GitHub importer — they leave personal-data transit, prompt retention, secret/file reads, prompt injection, and non-LLM egress unconstrained.
- 2026-07-11 — Issue #25 generated-voice eval coverage: §21.27–§21.29 exercise employment-framing rejection, permanent-identity rejection, and unchanged passage of an uncomfortable evidence-grounded mirror claim, with matching §24.31–§24.33 criteria; verifiers label but never rewrite under the single-pass rule. Rejected alternative: infer coverage from §24.8/§24.10 and neighboring flattery/evidence evals — none exercises these three distinct §16 behaviors or proves unchanged prose.
- 2026-07-11 — PR #44 review: the §29.4 deny gate governs the source-acquisition channel, not every file open — the service's reads of the §14.1 workspace internals (`.exp2res/config.toml`, the SQLite database file) are internal I/O outside the gate, while those same paths stay denied as `path`/`uri`/payload locators and their content stays out of prompts. Rejected alternative: a literal every-open gate — the first config load that selects the provider and ignore patterns becomes self-denying and circular, and the SQLite carve-out already concedes that internal reads are not source acquisition.
- 2026-07-12 — Issue #32 evidence-strength closure: `EvidenceStrength` is closed over V1 producers — `weak_note`, `code_artifact`, `test_or_demo`, `external_feedback`, and `verified_outcome` are retired because no §14 flow assigns them; §14.2–§14.5 produce the five retained values, and a retired value returns only together with its producing flow (the #21 rule). Rejected alternative: keeping them as documented reserved values — a closed Literal with unreachable members is exactly the producerless-enum disease #21 removed, and §13.3/§16 calibration would have to define semantics for values no row can carry.
- 2026-07-12 — Issue #58 calibration model: evidence-to-confidence calibration is capability-based, not a total order (new §9.4) — each retained `EvidenceStrength` defines an evidential scope; deterministic ceilings bound `Confidence` (same-log items count once, repeated owner assertion never corroborates, `high` requires two distinct raw logs including a non-`manual_claim` item), propagation caps bound signals and claims by their sources plus Stage 7 coverage judgment, a candidate above a cap is invalid structured output under §15.1 and is never silently lowered, and strength never authorizes ownership/metric/production/temporal/employment content (§16.4–§16.8 fail closed independently). Rejected alternatives: a numeric or total strength order — the values are qualitatively incomparable and a rank invites cross-scope authorization; free LLM-judged confidence with cited inputs — two conforming implementations could not agree on a defensible range.
- 2026-07-12 — Issue #40 assessment scopes: `custom` is retired from `AssessmentScope` — producerless (no §14.9 command form, no `scope_target` semantics, no replacement-key definition); `--scope` explicitly accepts the remaining §10 values with `global` as the default and `project` alone carrying a scope target. A custom scope returns only together with its command form, target requiredness, and replacement-key semantics (the #21 rule). Rejected alternative: adding `--scope custom --target <label>` — a free-label scope duplicates project scoping without a distinct V1 consumer while adding one-current-per-scope identity questions.
- 2026-07-12 — Issue #57 detection references: Stage 4 targets use the closed `DetectionRefType` alias (`raw_log`, `evidence_item`, `experience_fact`) on `Contradiction.left/right_ref_type` and `GapQuestion.target_type`, and the broad `EntityRefType` is retired — Stage 4 was its only producer, and its four upper-layer members were persistable in the model while impossible under §13.4/§15.8; the persisted model, §12 validator, and §15.8 producer now expose one identical closed target domain. A retired breadth returns only with a genuine polymorphic producer and consumer (the #21 rule). Rejected alternative: keeping `EntityRefType` and rejecting upper-layer members in prose — the model and storage validator would keep advertising values no legal producer can emit.
- 2026-07-12 — Issue #36 branch JD anchor: `ResumeBranch.job_description_id` is required (`str`), derives a `NOT NULL` FK to retained `job_descriptions` under §12 rule 6, and joins the rule 10 table — the JD half of the anchor pair now matches the snapshot half closed by #7, and the optional-field workaround prose in §11.12/§13.10/§14.10/§18 is removed. Rejected alternative: keeping `Optional` with a producer-only copy rule — a V1 branch cannot legitimately omit or outlive the job description that defined its relevance matching, and model-level optionality on a trigger-required anchor is exactly the drift #7 closed.
- 2026-07-12 — Issue #63 Stage 4 trigger: one generation command — `exp2res detections generate` — is the sole Stage 4 trigger, `gaps`/`contradictions` keep inspection and answer subcommands only (`gaps list` added for answer discovery), so no command form implies a half-replacement; a successful run retains the prior current generation when the validated candidate is content-equivalent over detector-authored fields of both sets (answered-gap state is service lifecycle, excluded from comparison, and survives retention) and otherwise replaces both sets and supersedes every upper layer in one transaction, with help/output reporting the both-sets side effect and invalidated classes; exit-code detail stays with the #55 CLI result contract. Rejected alternatives: keeping `gaps generate`/`contradictions generate` as documented both-sets aliases — command names that promise partial operations while replacing both sets are a standing operator trap; a genuine partial-set producer — it needs a different Stage 4 contract with merge rules, generation identity, and answered-gap carry-forward, contradicting the #23 complete-generation model; always-replace without an equivalence rule — an unchanged rerun would supersede referenced detections and needlessly invalidate every current snapshot and branch.
- 2026-07-12 — PR #66 review: Stage 4 retention requires an all-unanswered current generation — `answered`/`answer_log_id` never enter the content-equivalence comparison, and a generation containing an answered gap is always replaced with each replacement gap starting open, preserving §14.7's never-re-link rule: a re-emitted gap is genuinely still open, and an insufficient answer cannot hide an open unknown from Stage 6. Rejected alternative: letting answered state survive retention — an answer captured but not yet extracted would freeze `answered = true` on a gap the detector still derives from inputs that include that answer.
- 2026-07-12 — PR #66 review: §11.9's continuing-conflict replacement-row rule is scoped to replacing regenerations, deferring the retain-or-replace choice to §13.4 as the single lifecycle authority; a content-equivalent all-unanswered rerun retains current contradiction rows. Rejected alternative: keeping the unconditional replacement-row sentence — a no-op rerun would have two normative outcomes, splitting row identity, history, and invalidation across implementations.
- 2026-07-12 — PR #66 review r2: "sole Stage 4 trigger" is scoped to the sole direct detection-generation command — §14.12 lifecycle recomputation (correction and deletion rebuilds) invokes the same Stage 4 under §13.4/§13.13, so the single-command rule constrains the CLI surface, not the required rebuild paths. Rejected alternative: leaving "sole trigger" unscoped — an implementation could read it as forbidding the mandatory Stage 4 regeneration inside recompute.
- 2026-07-12 — PR #66 review r3: contradiction content-equivalence compares the two detection references as an unordered pair — the detector has no canonical side ordering, so a swapped left/right rerun is the same conflict and must not churn current generations. Rejected alternative: imposing a canonical side ordering on the §15.8 producer — it constrains detector output for a property only the comparison needs.
- 2026-07-12 — PR #66 review r3: §9.4 propagation caps are total — the maximum over an empty source list is `unknown`, so a sourceless signal or claim has a defined structural cap and implementations cannot diverge, while §13.7 rule 1 still fails sourceless claims at verification. Rejected alternative: min-length source lists on §11.5/§11.6 — a model change beyond this calibration fix that would forbid counter-evidence-only signal shapes the models deliberately allow.
- 2026-07-12 — PR #66 review r4: Stage 4 retention is scoped to its own stage — retention supersedes nothing of Stage 4's making and a direct §14.7 invocation invalidates nothing upstream, but inside the §14.12 flow the downstream Stage 5–7 regeneration still follows §13.13; §21.19 is rephrased to the retain-or-replace lifecycle so an unchanged conflict is preserved by whichever path runs and a prior row supersedes only through a replacing regeneration. Rejected alternative: skipping Stage 5–7 when recompute retains detections — Stages 5–6 are complete-replacement stages whose retention semantics are a separate decision, not a side effect of detection equivalence.
- 2026-07-12 — PR #66 review r4: the §9.1 ontology line for Contradiction now matches Stage 4's closed target domain — a detected conflict between effective source records and current facts — instead of promising claim/signal conflicts no producer can persist under DetectionRefType. Rejected alternative: adding upper-layer ref types back — reopens the #57 producerless breadth.
- 2026-07-12 — PR #66 review r5: the §9.4 `high` fact ceiling is reachable in V1 only within one correction lineage — under §13.3's single-lineage extraction the linked set spans multiple raw logs only as root plus corrections, so `high` requires an imported non-`manual_claim` root corrected by the owner; cross-lineage corroboration remains producerless (§12.4) and cannot create the spanning set. Rejected alternatives: a V1 cross-lineage corroboration flow — a new producer beyond this decision; dropping the `high` branch — it would make `high` a producerless `Confidence` member and foreclose the corrected-import case that is legal today.
- 2026-07-12 — PR #66 review r5: the §15.5 assessment-verifier input carries `source_evidence_items` — the duplicate-free EvidenceItem set reached through the supplied source facts — so Stage 7 judges claim confidence under §9.4's strength/scope frame from supplied data, never hidden state. Rejected alternative: dropping the §13.7 rule 2 strength/scope judgment — it is the semantic half §9.4 explicitly assigns to Stage 7.
- 2026-07-12 — PR #66 review r6: §15.5 `source_facts` is the claim's provenance closure — its `source_fact_ids` plus every listed source signal's supporting and counter facts — with `source_evidence_items`/`source_logs` reached through that closure, so a signal-only claim still supplies the underlying evidence for §13.7 rule 2's strength/scope judgment. Rejected alternative: requiring the writer to restate underlying facts on every claim — it duplicates signal provenance into claim source lists §11.6 deliberately keeps separate.
- 2026-07-12 — Issue #62 verifier input closure: the §15.5 assessment-verifier input is exact and deterministic — `source_signals` is the claim's duplicate-free `source_signal_ids` set, `source_facts` the provenance closure, `source_evidence_items`/`source_logs` the evidence items and retained raw logs reached through it, every array duplicate-free and ID-ordered; Stage 7 assembles exactly that bundle from current rows and fails closed on a missing, superseded, duplicate, or extra member before any provider call, and §29.3's §15.5 row now names the closure including linked evidence items. Rejected alternatives: adding current contradictions to the per-claim input — closure counter facts already carry the conflict evidence, snapshot completeness is §13.7 check 9's structural property, and unrelated detection rows would widen the declared transmission surface; replacing §13.7 rule 2 with a deterministic local strength check — §9.4 explicitly assigns the semantic coverage judgment to Stage 7.

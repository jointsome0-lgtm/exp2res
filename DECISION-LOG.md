Format: `YYYY-MM-DD — decision in one phrase; rejected alternative and why.`
New entries are linted by `python3 scripts/check_decision_log.py --baseline 2026-07-15 DECISION-LOG.md`;
entries on or before 2026-07-15 are a grandfathered baseline.

## Decision Log

- 2026-07-03 — Keep the SDD as a single file navigated via the § Index (map-as-interface);
  a physical split into a map file plus per-section files is deferred until after the
  structural dedup pass, and if done, must be a purely mechanical commit (concatenated
  section files must reproduce the original). Revisit triggers: code lands and sections
  graduate into living docs/tests; full-pass reviews start hitting context limits; parallel
  per-section editing becomes the norm; the file exceeds ~30–40K tokens despite dedup.
  Rejected alternative: splitting now — point reads already load only the needed section
  (§ Index + grep), a split does not fix the actual pain (cross-section duplication and
  drift), and splitting before dedup would migrate content about to be merged or deleted.
- 2026-07-04 — Structural dedup pass: §12 recognized as derived from §11 and replaced with
  derivation rules — with the exception of fact_sources and processing_runs, plus (found
  during the loss check) contradictions, gap_questions, job_descriptions, resume_branches,
  which also have no Pydantic counterpart and keep normative DDL; §22 compressed to a
  phase → §13-stages → definition-of-done table (commands live in §14; `exp2res logs list`
  rehomed to §14.11); anti-flattery mantras consolidated into §16 (+§5.10, §27) with
  one-line references from §4.3, §5.1, §15.4, §21.1, §25.2. Version not bumped — no
  decisions changed. Rejected alternative: leaving duplicates as-is — duplication cost is
  paid again in every polishing session and cross-copy drift risk grows.
- 2026-07-04 — Executed the deferred physical split (see 2026-07-03): `SDD.md` is now the map (§ Index + numbering rules), each top-level § lives in `spec/NN-slug.md`, decisions in `DECISION-LOG.md`; § numbering and all content unchanged — head + `spec/` files + log concatenate back to the pre-split file byte-for-byte (verified). Trigger: context root — file boundaries enforce the section-scoped reading the convention only requested (the exp2res monolith already exceeded the default 2000-line read window, silently truncating whole-file reads; split kept symmetric across SDD-stage repos). Rejected alternative: single file + convention — conventions don't bind agents, structure does.
- 2026-07-04 — Map aligned with the atlas pattern: § Index preamble gains "retired numbers are never reused" plus the registry (§12.1–§12.3, §12.5, §12.6, §12.9, §12.12 — the dedup's derivable tables); header Date field dropped (stale one day after writing — git is the date authority); henceforth a § edit updates its map line and status in the same commit. Rejected alternative: hand-maintained Date and per-§ retirement notes only — a duplicate of git metadata that had already drifted, and a registry readers can't find from the map.
- 2026-07-04 — §20 rewritten from a 112-line speculative file tree to placement principles + a normative skeleton (issue #1, the atlas §8 pattern): grep across spec/ showed no other § relies on any source path in the tree — the only load-bearing paths (out/*, .exp2res/, logs/) are runtime workspace owned by §14.1/§13.12 and were never in it, and docs/SDD.md was already falsified by the physical split. Skeleton keeps the root the repo already has plus the layer packages; module/test/example names now derive from §13, §21+§27, §19/§14 instead of being pre-named. Rejected alternative: keeping the full tree — it restates §13/§21/§27 at 112-line cost per full pass and drifts the moment code lands.
- 2026-07-04 — Issue #2: the four §9.1 ontology entities that had only DDL — Contradiction,
  GapQuestion, JobDescription, ResumeBranch — get Pydantic models (§11.9–§11.12) and §12 now
  derives their tables (§12.7, §12.8, §12.10, §12.11 retired); normative DDL remains only for
  the two non-ontology storage artifacts (fact_sources, processing_runs), making §12's
  exception list principled instead of enumerated. Grounds: they cross the LLM boundary
  (§15.3/§15.4 inputs, stage-4/8 outputs) where §15.1 mandates Pydantic validation; §11 already
  holds typed refs to them (AssessmentSnapshot.gap_question_ids/contradiction_ids,
  ResumeBullet.branch_id); and §5's "contradictions are first-class" is hollow if Contradiction
  is an entity without a domain model. Supporting changes: §10 gains EntityRefType and
  GapTrigger (now the canonical home of §13.4's trigger list); derivation rules gain
  bool→INTEGER and polymorphic-ref-without-FK; §11 preamble fixes its scope as "persisted
  ontology entities" and records why VerificationFinding stays contract-level (§15.5/§15.7,
  stored denormalized on its targets). Rejected alternative: declaring the four
  storage-level-only in §12 — saves no spec volume (models replace equal-sized DDL), leaves a
  third of the ontology without domain representation, and defers the same modeling work to
  implementation time where it would happen ad hoc and unreviewed.
- 2026-07-05 — Issue #24: §20 principle 3 points job descriptions to §13.8
  (Stage 8 — Job Description Parsing) instead of §13.11 (Resume Verification).
  Rejected alternative: leaving the pointer for readers to infer — it sends
  `examples/` population to the wrong pipeline stage.
- 2026-07-05 — Issue #6: §10 is the canonical, mechanically extractable home for every enum/Literal value list used by §11 persisted models, and §13/§27 are reference-only mirrors where they mention canonical lists or rules; §11 replaces inline Literals with named §10 aliases, §13.5/§13.6 point to the §10 alias plus §11 field instead of listing values, and §27 points to canonical §16 rule numbers with a same-commit sync rule. The shape is intentionally compatible with a post-MVP generated docs/schema/lint pass, but no generator, linter, separate registry file, runtime schema tooling, generated documentation, or MVP test infrastructure is introduced now. Version not bumped — no enum values, verifier behavior, schema fields, or export safety rules changed. Rejected alternative: leaving model-local literals or prose restatements — it preserves multiple homes for the same list and repeats the atlas drift failure; rejected alternative: implementing generated enum docs now — useful later, but premature before MVP and likely to turn an SDD dedup issue into tooling work.
- 2026-07-11 — Issue #11 evidence classification: §10 owns the ten-value `EvidenceStrength` taxonomy, `EvidenceItem.strength` is typed by it, and GitHub-commit evidence recording (now Stage 1 under issue #19) uses `commit_or_pr` for the same commit as §19.3; `EvidenceItem.evidence_type` is removed because source origin already lives on `RawLog` and the field had no distinct values or consumers. Rejected alternative: introduce a second `EvidenceType` taxonomy — it would duplicate the strength/origin axes and enlarge v1 without a demonstrated use.
- 2026-07-11 — Issue #11 fact-source relations: `fact_sources.support_type` is constrained to `direct` or `corroborating`, independently of `EvidenceStrength`, and every fact requires a direct source. Rejected alternative: leave an open string or reuse evidence-strength values — either preserves an undefined value space or collapses source relationship and source strength into one axis.
- 2026-07-11 — Issue #16 temporal shape: `OccurredAt.precision` is the sole discriminator, its value determines the legal `start`/`end` shape, and the redundant `OccurredAt.kind`, `OccurredAtKind`, and `occurred_kind` storage column are removed. Rejected alternative: persist both fields plus a legal-pair matrix — it stores derivable state and adds a consistency surface with no v1 information gain.
- 2026-07-11 — Issue #16 confidence axes: `Confidence` types general confidence on `ExperienceFact`, `SelfSignal`, and `SelfClaim`, while `TemporalConfidence` is reserved for `OccurredAt.confidence`. Rejected alternative: reuse `TemporalConfidence` because the members currently match — it conflates confidence in when something happened with confidence in what a derived claim asserts.
- 2026-07-11 — Issue #15 ownership comparison: `OwnershipLevel` is a normative total order from `unknown` through `led`, with `unknown` weakest and claim confidence kept independent; verifiers fail closed when ownership language cannot be normalized. Rejected alternative: make `unknown` incomparable — a verifier could not conservatively decide whether any known ownership phrase is an upgrade.
- 2026-07-11 — Issue #15 temporal comparison: non-range `TemporalPrecision` values compare by a weak-to-strong rank and normalized maximum uncertainty width, while ranges compare by actual width with approximate bounds weaker at equal width. Rejected alternative: compare raw enum positions — range precision depends on duration, so a fixed position would misclassify cases such as a two-day range versus a month.
- 2026-07-11 — PR #30 review: zero-width ranges are illegal — `date_range`/`approximate_range` require `end` strictly after `start`; an instant is `exact_datetime`/`exact_day`. Rejected alternative: define a precision-implied width for zero-width ranges — it would give one moment two representations and make §16.7's equal-width exactness rule ambiguous.
- 2026-07-11 — Issue #26 creation timestamps: every persisted entity model except `RawLog` carries system-assigned `created_at` at first persistence, `RawLog.recorded_at` keeps its §5.4 meaning, and §12 derives every entity timestamp without a `resume_bullets` exception. Rejected alternative: infer creation solely from `processing_runs` — §15.1 guarantees run metadata only for LLM calls and §12.13's untyped output IDs cannot identify the first persistence of every entity without a larger provenance design.
- 2026-07-11 — Issue #19 pipeline identity: a stage requires a §14 trigger and a downstream-referenceable persisted business result; evidence recording now occurs atomically in Stage 1, relevance matching occurs inside Stage 10 and persists on `ResumeBullet`, and §13.2/§13.9 are retired without renumbering, leaving ten active stages. Rejected alternative: preserve paper stages or force exactly nine by hiding an independently triggered persisted verifier/export operation — either keeps the original contradiction or violates the same stage criterion.
- 2026-07-11 — Issue #20 CLI authority: §14 is the only home of command forms, noun-led generation groups use an explicit `generate` verb, required paths stay positional, and stored-record selectors plus generation/export context use named options; §13 carries semantic trigger references only. Rejected alternative: retain per-stage command copies or bare-noun generation — both preserve grammar drift surfaces already falsified by assessment verification and contradiction commands.
- 2026-07-11 — Issue #21 entry producers: V1 keeps only the eight `EntryType` values with explicit capture/import producers, restricts Atlas to artifact references and GitHub to commits, and makes `import file` a design-document importer; six producerless or contractless values are retired and may return with contracts. Rejected alternative: add speculative note, competition, learning, Atlas-trail, PR, and issue importers — it enlarges V1 to preserve unused enum members.
- 2026-07-11 — Issue #21 raw-source boundary: `llm_inferred` is removed from `SourceType`, and LLMs may neither create nor mutate `RawLog`; inference begins at `ExperienceFact` and remains attributable to processing runs. Rejected alternative: define an LLM raw-log producer — it would place derived interpretation in the immutable user/imported truth layer.
- 2026-07-11 — Issue #21 export claims: `export_claim` is removed from `ClaimKind`; resume bullets remain a distinct projection carried by source links and verification fields, while fact and assessment contracts own the remaining claim kinds. Rejected alternative: add a constant-like `claim_kind` field to `ResumeBullet` — it adds no v1 distinction or verifier behavior.
- 2026-07-11 — Issue #22 runtime workspace: V1 initialization creates only `.exp2res/` and `out/`; SQLite owns raw/evidence records, while supplied files remain in place and their paths are stored as provenance references. Rejected alternative: copy inputs into managed `logs/` or `evidence/` directories — it adds a second file lifecycle and ambiguous source-of-truth semantics without a V1 consumer.
- 2026-07-11 — PR #31 review: the assessment verifier output (§15.5) carries `counterevidence` — contrary-evidence statements grounded in supplied sources, empty when none — so Stage 7 can persist `SelfClaim.counterevidence` through a validated payload. Rejected alternative: keep the field outside the verifier contract — no other flow produces counterevidence, leaving §11.6's field unfillable under §15.1's fail-closed rule.
- 2026-07-11 — PR #31 review: `gaps answer` sets `GapQuestion.answered = true` together with `answer_log_id` in one transaction, and `answered` is true iff `answer_log_id` is set. Rejected alternative: drop the bool as derivable from `answer_log_id` — §12 rule 4 already derives a persisted NOT NULL column from it, and removing a §11.10 model field is a model change beyond this command-note fix.
- 2026-07-11 — Issues #3 and #9 data lifecycle: raw records are immutable to automation but owner-deletable; corrections append linked records and replace one coherent current derived generation while preserving superseded snapshots as inspectable history, whereas owner deletion hard-deletes the selected raw record, globally purges all derived database generations, verifies managed-export removal with residual-path reporting, and rebuilds from surviving sources without allowing cleanup/rebuild failure to undo database deletion. Rejected alternatives: warn-and-skip or selective graph deletion — either can retain private derived text or present an incomplete model as current because V1's JSON and implicit dependencies cannot prove a selective purge complete.
- 2026-07-11 — Issue #17 provenance integrity: V1 keeps upper typed ID lists as JSON but resolves every target transactionally before persistence, while `fact_sources` uses non-null EvidenceItem links and derives raw-log provenance through `EvidenceItem.raw_log_id`, allowing multiple evidence items from one raw log; unresolved or inconsistent links fail the producing run atomically. Rejected alternatives: a generic link table cannot provide cross-table SQLite foreign keys, per-field tables add many storage artifacts without a V1 query need, and a surrogate key plus nullable evidence preserves a producerless raw-only state and null-aware uniqueness while making evidence-strength calibration ambiguous.
- 2026-07-11 — PR #34 review: `corrects_log_id` is a capture-time requirement, not a standing invariant — a correction orphaned by owner deletion (`ON DELETE SET NULL`) is a valid model state and roots its own lineage, accepted by hydration, §12 rule 10, and the §13.13 rebuild. Rejected alternatives: reclassifying the orphan's `entry_type` — a system rewrite of a raw record §5.3 forbids; purging the orphaned lineage — deletes records the owner did not select, and correction text is self-contained by construction precisely so it can stand alone.
- 2026-07-11 — PR #34 review: the §16.1 current-chain requirement binds current rows and verification/export inputs only; superseded rows are exempt inspect-only history whose references legitimately point at superseded targets, and §12 rule 9 keeps them out of all consumers. Rejected alternative: apply the chain rule to every persisted row — it would invalidate preserved history or force a purge on every correction, contradicting §11's history-retention rule.
- 2026-07-11 — PR #34 review: gap answers are self-contained at capture — `gaps answer` copies the question text and reason into the answer's `RawLog.metadata`, so surviving answers stay interpretable after Stage 4 regeneration or the §13.13 reset, and links to regenerated questions are never re-created (a resolved uncertainty no longer fires its trigger; a regenerated gap is genuinely open). Rejected alternatives: preserving `GapQuestion` rows through the reset — retains derived text the purge must remove; post-rebuild re-linking — the purged question row no longer exists to match against.
- 2026-07-11 — PR #34 review: the fact-extractor input (§15.2) carries `RawLog.metadata` through unmodified, so the §14.7 gap-answer question context reaches extraction — self-containment must hold in the pipeline, not only at rest. Rejected alternative: require owners to restate the question inside their answer text — the system already holds the context and manual restatement is unreliable.
- 2026-07-11 — Issue #8 verification semantics: §16.11 is the sole operational home for `VerificationStatus` meanings and role-aware consumer allowlists; Stage 7 reduces snapshot status over all claims including a required `narrative_summary` claim that exactly carries snapshot summary prose, assessment exports preserve visibly labeled uncertainty, and only `supported` claims and bullets may become resume content. `ExperienceFact` loses its producerless status field. Rejected alternatives: denylist gates, which let unnamed unsafe states pass, and a new fact-verifier path introduced only to justify an unused field.
- 2026-07-11 — Issue #7 resume anchoring: every `ResumeBranch` requires the exact current `AssessmentSnapshot` selected by §14.10's required `--snapshot` option, with no implicit latest default; branch, bullet, and source-claim consistency is validated before persistence and export. Rejected alternative: optional or inferred anchoring — it is ambiguous with multiple snapshots and permits the assessment and export layers to collapse.
- 2026-07-11 — Issue #18 review gates: V1 defines review as Stage 7/11 verifier gating of assessment and resume projections before their consumers, not as an owner verdict on regenerated derived rows; semantic promotions remain explicit and traceable, and producerless `SourceType.user_confirmed` is removed. Rejected alternative: durable owner confirm/dispute raw records — they require a new entry producer, verdict payload, stable matching across regenerated claim IDs, and precedence rules, enlarging V1 into a separate review subsystem.
- 2026-07-11 — Issue #23 contradiction lifecycle: V1 removes `resolved`/`dismissed` workflow state and `resolution_note` from derived contradiction rows; Stage 4 alone always replaces the complete current contradiction set, every current snapshot renders that set without scope filtering, and prior detections remain superseded history outside the owner-deletion privacy reset. Rejected alternative: resolve/dismiss commands backed by raw verdict records — regenerated contradiction IDs have no stable identity, so durable application requires semantic matching and precedence machinery, while mutating a derived row would silently lose the verdict on regeneration.
- 2026-07-11 — Issue #27 verifier mechanics: V1 verification is one validated semantic pass per current claim or bullet; §15.1 retries only invalid structured output, verifier findings and suggested rewrites are returned to the owner, and verifiers never invoke writers or mutate or drop derived prose. Revised wording requires an explicit Stage 6/10 replacement generation. Rejected alternative: an automatic writer-verifier repair loop — it requires generation orchestration, termination and gap-production rules across Stages 4, 6, 7, 10, and 11, enlarging V1 and conflicting with the superseding lifecycle.
- 2026-07-11 — Issue #10 resume generation uses one isolated §15.6 invocation per bullet with only that bullet's evidence and supported claims, typed JD requirements, exact provenance sets, and all writer-settable ResumeBullet fields. Rejected alternative: retain the historical-version pointer, batch bullet contexts, or pass snapshot title/summary prose — these leave no recoverable contract, expose cross-bullet claim inputs, or recreate the unverified wording channel closed by PR #35.
- 2026-07-11 — Issue #13 fact time inherits the governing RawLog placement, permits only explicitly evidence-backed contained narrowing, and never exceeds in-context temporal precision or governing temporal confidence; Stage 5 consumes current facts, their linked evidence items, and contradictions, with gap answers reaching it only through Stage 3 and no prior-signal input. Rejected alternative: literal source-copy-only time or direct gap-answer/existing-signal inputs — the former discards stronger supplied temporal evidence, while the latter bypasses fact provenance or mixes replacement generations.
- 2026-07-11 — Issue #14 Stage 4 semantic detection and Stage 8 JD parsing are LLM-backed under appended §15 contracts, ParsedJD owns service-assigned stable typed requirement IDs, and matching remains internal to Stage 10 with retired Stage 9 unchanged. Rejected alternative: unspecified deterministic semantics, model-authored/untyped parsed identity, or a revived matching stage — each leaves an LLM boundary unvalidated, makes service failures model-retryable, or contradicts PR #31's stage model.
- 2026-07-11 — Issue #12 assessment unknowns use the complete current unanswered Stage 4 set in existing typed AssessmentSnapshot.gap_question_ids instead of a parallel prose field, project scope_target stores the §14.9 selector without changing the AssessmentScope replacement key, and §17 renders all claim counterevidence with visible status. Rejected alternative: writer-selectable unknown strings, answered gaps, or metadata storage — these permit uncertainty omission, misrepresent answered questions, duplicate gap state, or recreate the separately unverified snapshot prose channel closed by PR #35.
- 2026-07-11 — Issue #4 verification separates generated voice from source voice: structure is validated everywhere, every untagged LLM string is generated voice, and only typed byte-verified source segments escape §16.2–§16.10 without owner/system-of-record rewriting. Rejected alternative: trusting quote-like text or scanning concatenated payloads without origin boundaries — either launders model prose as source or falsely treats user/import text as a system claim.
- 2026-07-11 — PR #38 review: snapshot gap references are synthesis-time state — `gaps answer` never supersedes or fails a current snapshot; the answered-exclusion rule binds only the Stage 6 write boundary, §17 renders a post-synthesis answer as answered-since-synthesis context, and only the next generation excludes the row. Rejected alternatives: invalidating dependent snapshots inside the answer transaction — it destroys a still-honest mirror over evidence the pipeline has not extracted yet; read-time failure on answered references — it turns encouraged owner activity into an export outage until regeneration.
- 2026-07-11 — PR #38 review: `gaps answer` applies §13's managed-output invalidation to the exports it stales — while a current snapshot references the answered question, enumerate `out/assessment/` and every dependent branch's managed set, attempt removal, report residual paths — superseding no row; snapshot and branches stay immediately re-exportable. Rejected alternative: leaving pre-answer artifacts in place — a current-looking export would keep presenting the question as open, violating the no-stale-current-output rule every other lifecycle transition obeys.
- 2026-07-11 — PR #38 review: Stage 4 consumes effective lineage evidence only — the raw logs and evidence items governing current extraction under §13.3 rule 10/§14.4, with factless effective records still targetable; content displaced by a selected correction is neither input nor target. Rejected alternative: the complete retained raw/evidence set — the detector would regenerate current contradictions and gaps from exactly the text the correction flow removed from the current fact generation, resurrecting resolved conflicts §14.4 exists to retire.
- 2026-07-11 — PR #38 review: §16.2–§16.10 are owner-referential — parsed external-demand text (§15.9) stays generated voice for structure and parse fidelity, but faithfully preserved demand wording characterizes the vacancy, cannot make `jd add` unpersistable, and is never force-rewritten; asserting the owner meets the demand stays fully bound wherever it appears. Rejected alternatives: byte-verified source-span fields on ParsedJD — §15.9's required normalization and modality preservation are not verbatim substrings; treating demand text as owner-claim voice — ordinary JDs ("expert Python") force meaning-destroying rewrites of the employer's requirement.
- 2026-07-11 — Issue #41 contract-example notation: each entity's full-shape example lives once at its producing contract (§15.2 facts/evidence/logs, §15.4 signals/gaps/claims, §15.9 ParsedJD); other examples elide repeated bodies to `<id: complete §NN Model>` placeholder strings defined in §15.1, with no normative change. Rejected alternatives: full objects in every input example — five ExperienceFact copies made each §11 field change a five-site edit and doubled §15's read weight; splitting the file — the one-§-per-file layout and the map's point-read flow are a standing convention.
- 2026-07-11 — PR #42 review: a §15.1 placeholder denotes the complete persisted §11 shape — the producer example plus exactly the service-set fields its prose names (§15.2: Stage 3's id/created_at/superseded_at; §15.9: Stage 8's JobDescription.id/created_at and JDRequirement.id) — because producer examples are deliberately model-emitted subsets. Rejected alternative: restoring a full persisted example per consumer input — recreates the duplication #41 removed while §11 already carries the normative shape.
- 2026-07-11 — PR #42 review r2: §15.1 producer references corrected (signal → §15.3, gap/contradiction → §15.8; persisted-row example sites named explicitly), and a behavior-bearing example object — one whose concrete content the same example's output depends on — is never elided, so §15.8 shows its fact and raw log in full. Rejected alternative: placeholder field-overrides for load-bearing content — inline deltas recreate exactly the example divergence the notation exists to prevent.
- 2026-07-11 — Issue #5 security/privacy boundary: the owner-controlled local workspace is Exp2Res's only canonical persistence domain; only foreground user-started runs may transmit the exhaustive typed §15.2–§15.9 payloads to an explicitly selected provider, while all other LLM/network calls, outbound telemetry/sync/push, secret or ignored context, and instruction-following from source data are forbidden; §14.5 therefore consumes a local GitHub payload, §21.30 tests JD injection, provider retention and false-but-structured source poisoning remain explicit residual risks, and any weakening requires another Decision Log entry. Rejected alternative: rely on the "local-first" header, provider abstraction, and network-shaped GitHub importer — they leave personal-data transit, prompt retention, secret/file reads, prompt injection, and non-LLM egress unconstrained.
- 2026-07-11 — Issue #25 generated-voice eval coverage: §21.27–§21.29 exercise employment-framing rejection, permanent-identity rejection, and unchanged passage of an uncomfortable evidence-grounded mirror claim, with matching §24.31–§24.33 criteria; verifiers label but never rewrite under the single-pass rule. Rejected alternative: infer coverage from §24.8/§24.10 and neighboring flattery/evidence evals — none exercises these three distinct §16 behaviors or proves unchanged prose.
- 2026-07-11 — PR #44 review: the §29.4 deny gate governs the source-acquisition channel, not every file open — the service's reads of the §14.1 workspace internals (`.exp2res/config.toml`, the SQLite database file) are internal I/O outside the gate, while those same paths stay denied as `path`/`uri`/payload locators and their content stays out of prompts. Rejected alternative: a literal every-open gate — the first config load that selects the provider and ignore patterns becomes self-denying and circular, and the SQLite carve-out already concedes that internal reads are not source acquisition.
- 2026-07-12 — Issue #32 evidence-strength closure: `EvidenceStrength` is closed over V1 producers — `weak_note`, `code_artifact`, `test_or_demo`, `external_feedback`, and `verified_outcome` are retired because no §14 flow assigns them; §14.2–§14.5 produce the five retained values, and a retired value returns only together with its producing flow (the #21 rule). Rejected alternative: keeping them as documented reserved values — a closed Literal with unreachable members is exactly the producerless-enum disease #21 removed, and §13.3/§16 calibration would have to define semantics for values no row can carry.
- 2026-07-12 — Issue #58 calibration model: evidence-to-confidence calibration is capability-based, not a total order (new §9.4) — each retained `EvidenceStrength` defines an evidential scope; deterministic ceilings bound `Confidence` (same-log items count once, repeated owner assertion never corroborates, `high` requires two distinct raw logs including a non-`manual_claim` item), propagation caps bound signals and claims by their sources plus Stage 7 coverage judgment, a candidate above a cap is invalid structured output under §15.1 and is never silently lowered, and strength never authorizes ownership/metric/production/temporal/employment content (§16.4–§16.8 fail closed independently). Rejected alternatives: a numeric or total strength order — the values are qualitatively incomparable and a rank invites cross-scope authorization; free LLM-judged confidence with cited inputs — two conforming implementations could not agree on a defensible range.
- 2026-07-12 — Issue #40 assessment scopes: `custom` is retired from `AssessmentScope` — producerless (no §14.9 command form, no `scope_target` semantics, no replacement-key definition); `--scope` explicitly accepts the remaining §10 values with `global` as the default and `project` alone carrying a scope target. A custom scope returns only together with its command form, target requiredness, and replacement-key semantics (the #21 rule). Rejected alternative: adding `--scope custom --target <label>` — a free-label scope duplicates project scoping without a distinct V1 consumer while adding one-current-per-scope identity questions.
- 2026-07-12 — Issue #57 detection references: Stage 4 targets use the closed `DetectionRefType` alias (`raw_log`, `evidence_item`, `experience_fact`) on `Contradiction.left/right_ref_type` and `GapQuestion.target_type`, and the broad `EntityRefType` is retired — Stage 4 was its only producer, and its four upper-layer members were persistable in the model while impossible under §13.4/§15.8; the persisted model, §12 validator, and §15.8 producer now expose one identical closed target domain. A retired breadth returns only with a genuine polymorphic producer and consumer (the #21 rule). Rejected alternative: keeping `EntityRefType` and rejecting upper-layer members in prose — the model and storage validator would keep advertising values no legal producer can emit.
- 2026-07-12 — Issue #36 branch JD anchor: `ResumeBranch.job_description_id` is required (`str`), derives a `NOT NULL` FK to retained `job_descriptions` under §12 rule 6, and joins the rule 10 table — the JD half of the anchor pair now matches the snapshot half closed by #7, and the optional-field workaround prose in §11.12/§13.10/§14.10/§18 is removed. Rejected alternative: keeping `Optional` with a producer-only copy rule — a V1 branch cannot legitimately omit or outlive the job description that defined its relevance matching, and model-level optionality on a trigger-required anchor is exactly the drift #7 closed.
- 2026-07-12 — Issue #63 Stage 4 trigger: one generation command — `exp2res detections generate` — is the sole Stage 4 trigger, `gaps`/`contradictions` keep inspection and answer subcommands only (`gaps list` added for answer discovery), so no command form implies a half-replacement; a successful run retains the prior current generation when the validated candidate is content-equivalent over detector-authored fields of both sets (answered-gap state is service lifecycle, excluded from comparison, and survives retention) and otherwise replaces both sets and supersedes every upper layer in one transaction, with help/output reporting the both-sets side effect and invalidated classes; exit-code detail stays with the #55 CLI result contract. Rejected alternatives: keeping `gaps generate`/`contradictions generate` as documented both-sets aliases — command names that promise partial operations while replacing both sets are a standing operator trap; a genuine partial-set producer — it needs a different Stage 4 contract with merge rules, generation identity, and answered-gap carry-forward, contradicting the #23 complete-generation model; always-replace without an equivalence rule — an unchanged rerun would supersede referenced detections and needlessly invalidate every current snapshot and branch.
- 2026-07-12 — PR #66 review: Stage 4 retention requires an all-unanswered current generation — `answered`/`answer_log_id` never enter the content-equivalence comparison, and a generation containing an answered gap is always replaced with each replacement gap starting open, preserving §14.7's never-re-link rule: a re-emitted gap is genuinely still open, and an insufficient answer cannot hide an open unknown from Stage 6. Rejected alternative: letting answered state survive retention — an answer captured but not yet extracted would freeze `answered = true` on a gap the detector still derives from inputs that include that answer.
- 2026-07-12 — PR #66 review: §11.9's continuing-conflict replacement-row rule is scoped to replacing regenerations, deferring the retain-or-replace choice to §13.4 as the single lifecycle authority; a content-equivalent all-unanswered rerun retains current contradiction rows. Rejected alternative: keeping the unconditional replacement-row sentence — a no-op rerun would have two normative outcomes, splitting row identity, history, and invalidation across implementations.
- 2026-07-12 — PR #66 review r2: "sole Stage 4 trigger" is scoped to the sole direct detection-generation command — §14.12 lifecycle recomputation (correction and deletion rebuilds) invokes the same Stage 4 under §13.4/§13.13, so the single-command rule constrains the CLI surface, not the required rebuild paths. Rejected alternative: leaving "sole trigger" unscoped — an implementation could read it as forbidding the mandatory Stage 4 regeneration inside recompute.
- 2026-07-12 — PR #66 review r3: contradiction content-equivalence compares the two detection references as an unordered pair — the detector has no canonical side ordering, so a swapped left/right rerun is the same conflict and must not churn current generations. Rejected alternative: imposing a canonical side ordering on the §15.8 producer — it constrains detector output for a property only the comparison needs.
- 2026-07-12 — PR #66 review r3: §9.4 propagation caps are total — the maximum over an empty source list is `unknown`, so a sourceless signal or claim has a defined structural cap and implementations cannot diverge, while §13.7 rule 1 still fails sourceless claims at verification. Rejected alternative: min-length source lists on §11.5/§11.6 — a model change beyond this calibration fix that would forbid counter-evidence-only signal shapes the models deliberately allow.
- 2026-07-12 — PR #66 review r4: Stage 4 retention is scoped to its own stage — retention supersedes nothing of Stage 4's making and a direct §14.7 invocation invalidates nothing upstream, but inside the §14.12 flow the downstream Stage 5–7 regeneration still follows §13.13; §21.19 is rephrased to the retain-or-replace lifecycle so an unchanged conflict is preserved by whichever path runs and a prior row supersedes only through a replacing regeneration. Rejected alternative: skipping Stage 5–7 when recompute retains detections — Stages 5–6 are complete-replacement stages whose retention semantics are a separate decision, not a side effect of detection equivalence.
- 2026-07-12 — PR #66 review r4: the §9.1 ontology line for Contradiction now matches Stage 4's closed target domain — a detected conflict between effective source records and current facts — instead of promising claim/signal conflicts no producer can persist under DetectionRefType. Rejected alternative: adding upper-layer ref types back — reopens the #57 producerless breadth.
- 2026-07-12 — PR #66 review r5: the §9.4 `high` fact ceiling is reachable in V1 only within one correction lineage — under §13.3's single-lineage extraction the linked set spans multiple raw logs only as root plus corrections, so `high` requires an imported non-`manual_claim` root corrected by the owner; cross-lineage corroboration remains producerless (§12.4) and cannot create the spanning set. Rejected alternatives: a V1 cross-lineage corroboration flow — a new producer beyond this decision; dropping the `high` branch — it would make `high` a producerless `Confidence` member and foreclose the corrected-import case that is legal today.
- 2026-07-12 — PR #66 review r5: the §15.5 assessment-verifier input carries `source_evidence_items` — the duplicate-free EvidenceItem set reached through the supplied source facts — so Stage 7 judges claim confidence under §9.4's strength/scope frame from supplied data, never hidden state. Rejected alternative: dropping the §13.7 rule 2 strength/scope judgment — it is the semantic half §9.4 explicitly assigns to Stage 7.
- 2026-07-12 — PR #66 review r6: §15.5 `source_facts` is the claim's provenance closure — its `source_fact_ids` plus every listed source signal's supporting and counter facts — with `source_evidence_items`/`source_logs` reached through that closure, so a signal-only claim still supplies the underlying evidence for §13.7 rule 2's strength/scope judgment. Rejected alternative: requiring the writer to restate underlying facts on every claim — it duplicates signal provenance into claim source lists §11.6 deliberately keeps separate.
- 2026-07-12 — Issue #62 verifier input closure: the §15.5 assessment-verifier input is exact and deterministic — `source_signals` is the claim's duplicate-free `source_signal_ids` set, `source_facts` the provenance closure, `source_evidence_items`/`source_logs` the evidence items and retained raw logs reached through it, every array duplicate-free and ID-ordered; Stage 7 assembles exactly that bundle from current rows and fails closed on a missing, superseded, duplicate, or extra member before any provider call, and §29.3's §15.5 row now names the closure including linked evidence items. Rejected alternatives: adding current contradictions to the per-claim input — closure counter facts already carry the conflict evidence, snapshot completeness is §13.7 check 9's structural property, and unrelated detection rows would widen the declared transmission surface; replacing §13.7 rule 2 with a deterministic local strength check — §9.4 explicitly assigns the semantic coverage judgment to Stage 7.
- 2026-07-12 — Issue #39 typed counterevidence: `SelfClaim.counterevidence` entries are typed `CounterevidenceItem` records — a verifier-authored `statement` plus a (`source_ref_type`, `source_ref_id`) polymorphic reference typed by the new `CounterevidenceRefType` (`raw_log`, `evidence_item`, `experience_fact`, plus `self_signal` from the r8+r10 widening) — validated under §12 rule 10 against the table the type selects, required to be a member of that claim's supplied §15.5 bundle, and duplicate-free by reference; the statement stays generated voice under §16.12 and §17 renders each entry with its navigable grounding reference. Rejected alternatives: keeping free counterevidence prose — the one evidential assertion the mirror leans on would stay unlinked while every other cross-layer assertion is typed; reusing `DetectionRefType` — it would give the Stage 4 alias a second producer and couple domains that have already diverged (the same reasoning that keeps `Confidence` and `TemporalConfidence` separate).
- 2026-07-12 — Issue #53 scope retirement: `career` and `learning` are retired from `AssessmentScope` — neither had a deterministic input-selection policy or a distinct V1 consumer, so scope collapsed into prompt wording; V1 keeps `global` and `project`, and a scope value returns only together with its deterministic selection semantics (the #21/#40 rule). Rejected alternative: defining ActivityContext-based career/learning filters now — every candidate partition either duplicated `global` or hard-coded a product judgment no V1 consumer needs.
- 2026-07-12 — Issue #53 scope semantics: Stage 6 input selection is structural and service-owned — `global` supplies every current fact and signal; `project` supplies subject facts whose canonicalized `project` equals the canonicalized target, every current signal referencing at least one subject fact, and the out-of-subject facts those signals reference as `context_facts`, keeping cross-target support and counterevidence visible; complete unanswered-gap and contradiction sets are never scope-filtered; writer citations must name supplied objects; an empty project subject fails before the provider call; Stage 7 receives scope/target and judges scope fit (§13.7 check 11). Replacement identity is the assessment view — (scope) for `global`, (scope, case-folded canonical target) for `project` — with targets canonicalized as NFC + trim, persisted pre-fold, matched via locale-independent case fold; distinct views are simultaneously current and the target stays a free label, not an entity reference. Rejected alternatives: scope as prompt-only context — two implementations could assemble materially different inputs and the writer could silently broaden or narrow scope; one-current-per-scope replacement — generating project B silently destroyed project A's current mirror; byte-exact target identity — invisibly different duplicate views (whitespace/NFC/case) and case collisions for per-view export paths on case-insensitive filesystems.
- 2026-07-12 — Issue #61 lifecycle boundary: lifecycle recomputation ends at Stage 5 — Stages 3–5 are parameterless shared derivations rebuilt automatically by §14.4/§14.11/§14.12, while Stage 6–7 assessment views and Stage 10–11 resume branches are explicitly parameterized projections regenerated only by their §14.9/§14.10 commands; the invalidating transaction captures the affected views and branches, the invoking command reports each view with its executable shell-quoted §14.9 command and each branch with captured context — name, retained job-description ID, former view — plus the §14.10 command shape, since no current `--snapshot` exists until the view is regenerated (after owner deletion as command output only), a crash-retry bare `recompute` reports zero current views instead of inferring a desired set, and §29.2's lifecycle authorization narrows from Stage 3–7 to Stage 3–5. Rejected alternatives: a persisted desired-view registry — a new owner-config entity with CRUD surface, post-deletion privacy questions, and a per-view partial-failure matrix, to automate a loop the owner can run explicitly; capturing the pre-invalidation plan — not crash-durable after the purge, and reconstructing it from history infers state the reset deliberately destroyed; scope selectors on `recompute` — cannot fix the argumentless correction and deletion flows.
- 2026-07-12 — PR #67 r8+r10: the §15.5 bundle adds the view context — `scope_signals` and `scope_facts`, the complete deterministic §13.6 selection re-derived from current rows — so §13.7 check 3 can see a contrary signal or fact the writer's account omits, while only the claim's closure deepens into evidence items and raw logs; an omitted contrary member grounds a non-passing status and may persist as a typed counterevidence reference (grounding domain = the supplied bundle; `CounterevidenceRefType` gains `self_signal`), and the §29.3 row widens accordingly. Rejected alternatives: passing every view fact's evidence/raw-log graph per claim — a raw-text transit blow-up against data minimization when fact rows already carry the conflict; a deterministic Stage 6 must-cite-every-input rule — forces filler claims and still cannot judge contrariness; reason-only omission reporting — the exported mirror would keep no navigable contrary source (persisted counterevidence is the typed channel §17 renders).
- 2026-07-12 — PR #67 r8: `ExperienceFact.project` is copied provenance (§13.3 rule 13) — it equals the governing source record's `project` exactly, including `None`, like the default `occurred` placement; an authored, renamed, or re-cased value is invalid structured output, because §13.6 project views select subjects through this field. Rejected alternative: selecting subjects through linked raw logs at Stage 6 — it duplicates the governing-record resolution §13.3 rule 10 already owns and lets a fact silently diverge from its stored project label.
- 2026-07-12 — PR #67 r9: §14.4 correction capture copies the target's `project` like its `OccurredAt` — the prompt starts from the target's value, copied exactly unless the owner explicitly replaces or clears it — so a text-only correction cannot silently strip the label the §13.6 project views select by from the lineage's governing record. Rejected alternative: root-project inheritance at extraction when the correction carries none — it makes clearing a mislabeled project inexpressible and splits governing-record semantics between capture and extraction.
- 2026-07-12 — Issue #37 per-view export namespace: assessment exports move from fixed `out/assessment/*` files to per-view directories `out/assessment/<view>/…`, where `<view>` is `global` or `project--<target>` with the case-folded canonical target percent-encoded (UTF-8 bytes outside `a-z 0-9 . _ -`); the slug reuses #53's view identity so simultaneously current views export side by side, verifier-change and gaps-answer removal target the affected view directories, and `--branch assessment` is rejected because its `out/<branch>/` directory would collide with the reserved namespace root. Rejected alternatives: last-export-wins on fixed paths — silently destroys the other view's export and leaves §13.7's removal rule unable to say whose files it removes; snapshot-ID directories — unambiguous but unstable across regenerations, leaving stale ID-named directories and no stable per-view location for removal rules.
- 2026-07-12 — Issue #60 entity identity: every top-level §11 entity `id` derives to a per-table `TEXT PRIMARY KEY`; typed polymorphic references select their table, while service-assigned opaque collision-resistant IDs are immutable, retried locally on retained-row collision without another LLM call, and never reused within their entity table for the workspace lifetime because opaque `processing_runs` telemetry outlives §13.13 purges; imported source identifiers remain provenance in `RawLog.external_ref`/metadata. Rejected alternatives: global cross-table uniqueness — every reference is already table-scoped by a closed type or foreign key, and SQLite cannot enforce it without a registry table; counter-derived IDs — the privacy purge can reset their surviving-row basis and enable reuse against retained telemetry; upstream identifiers as local IDs — source-system collisions and renames would control local identity.
- 2026-07-12 — Issue #45 schema compatibility: append-only `schema_meta` history is the sole schema-version authority, extending §12's storage-artifact inventory and superseding Issue #2's two-artifact count; compatibility is established before business I/O; only explicit `exp2res db migrate` may upgrade, after a verified WAL-inclusive backup, in one all-or-nothing transaction including foreign-key and one-current-generation validation; migrations preserve raw-record content, and §13.13 owner deletion removes managed backups or reports residuals. Rejected alternatives: `PRAGMA user_version` — it has no history and creates second-source drift; automatic on-open migration — any command could rewrite the canonical personal store as a side effect; per-migration commits — they expose partial upgrades and weaken failure recovery.
- 2026-07-12 — Issue #46 workspace concurrency: V1 permits multiple readers and exactly one workspace business writer; an exclusive OS advisory lock on `.exp2res/lock` is held across all business database transactions and coupled `out/` work, with persistent WAL, writer `synchronous = FULL` and `BEGIN IMMEDIATE`, bounded `busy_timeout`, single-snapshot read transactions, bounded-wait `workspace_busy` diagnostics, automatic process-death release, and a partial unique index backing current resume-branch names. Rejected alternatives: database transactions alone — they cannot serialize coupled managed-output filesystem work; PID or existence lock files — they require stale-lock heuristics after crashes; finer-grained per-table locks — they add deadlock surface without a V1 concurrent-writer need.
- 2026-07-12 — Issue #49 generation/run provenance and durable verifier findings: all eight recomputable tables gain storage-level `produced_by_run_id` plus one per-atomic-swap `generation_id`, never exposed at the LLM boundary or added to §29.3 transmissions; §13.13 lifecycle flows gain one orchestration run with child `parent_run_id` links; `processing_runs` gains privacy-safe execution identity — provider/model, prompt-policy and canonical input/validated-output hashes, provider request ID, token/retry/cost accounting, and stable failure codes, with identifiers, hashes, and counts but no content; and `VerificationFinding` becomes a persisted append-only §11 entity carrying each completed attempt's status, reason, unsupported phrases, typed counterevidence, and advisory rewrite as inspect-only history purged by owner deletion, while failed verifier runs persist telemetry only. This supersedes Issue #2's contract-level/denormalized-only finding treatment and the clauses saying `suggested_rewrite` is not persisted. Rejected alternatives: `generation_id = run_id` — one extract run atomically replaces multiple correction lineages; model-field provenance — it widens every §29.3 row and every §15 persisted-shape example for data the model must never see; keeping findings denormalized-only — re-verification history is uninspectable, failed attempts are indistinguishable, and advisory rewrites are lost; persisting findings from failed runs — §15.1 forbids partial invalid sets.
- 2026-07-12 — Issue #50 strict bounded object boundary: one canonical base-model policy applies `extra = forbid`, strict typing with ISO-8601-string-to-`datetime` as the sole JSON-boundary coercion, assignment validation, frozen-except-owned-lifecycle mutation, and UTF-8 sorted-key whitespace-free serialization for §12.13 hashes; entity metadata is narrowed to a bounded inert service/importer channel with named-key semantics; this extends PR #42's producer-shape supplement so LLM outputs never carry it and keeps §14.7's `question_text`/`question_reason` as the V1 named producer-consumer pair; normative size/count/depth and NUL/control hygiene are preflighted before provider calls and enforced at acquisition and hydration. Rejected alternatives: per-entity typed metadata models — two load-bearing keys do not justify eight closed models, and the named-key rule gives the same closure; lax/coercive validation — schema-shaped values would diverge across implementations; unlimited payloads — a valid response or import could exhaust memory or disk through schema-shaped denial of service.
- 2026-07-12 — Issue #64 correction displacement uses whole-record targets with scoped support survival: each retained correction displaces exactly the record named by `corrects_log_id`; effective lineage members are the non-displaced retained records, computed deterministically by the service with no LLM call, persisted resolution artifact, or ambiguity state; the latest effective member by (`recorded_at`, ID) governs source placement and project provenance. Displaced `raw_text`, copied question context, and `manual_claim` items never re-enter extraction or detection or support current facts, while non-`manual_claim` items owned by displaced records survive as prose-free support descriptors because their existence and attribution scopes outlive reinterpretation, preserving §9.4's corrected-import `high` ceiling; Stage 3 input is narrowed to effective records and their evidence plus those descriptors. Rejected alternatives: assertion-level correction resolution — V1 has no typed resolution artifact, LLM or owner producer, or merge semantics, so two implementations could not derive one effective set from prose; full displaced-record visibility with prompt-only discipline — stale content could be re-emitted; displacing imported evidence entirely — it contradicts §9.4 because a recorded commit does not stop existing when reinterpreted; latest-correction-replaces-whole-lineage — sibling corrections would silently discard each other's standing fixes.
- 2026-07-12 — PR #68 review: the prose-free displaced-record projection is universal across §15 inputs — items linked to displaced records serialize as §13.3 rule 10 support descriptors and displaced `RawLog` objects never serialize into prompts, with §29.3's Stage 3 row naming the descriptors and its verifier/writer rows narrowed. Rejected alternatives: forbidding facts from citing displaced support at all — it reopens the §9.4 corrected-import `high` ceiling #64 deliberately preserved; hydrating complete objects with prompt-side discipline — it is exactly the non-deterministic exclusion #64 rejected at Stage 3.
- 2026-07-12 — PR #68 review: per-call execution identity moves to the storage-only §12.15 `llm_calls` table, whose composite (`run_id`, `call_index`) primary key records one row per planned provider invocation with that invocation's hashes, request ID, token/cost accounting, and retry identity; `processing_runs` keeps run-level identity and remains the sole production-provenance anchor. Rejected alternatives: an aggregate hash-of-hashes or last-call-wins columns on the run row — lossy overwrite semantics the finding names; one run row per call — breaks the single `produced_by_run_id`/generation anchor of atomic swaps.
- 2026-07-12 — PR #68 review: owner-deletion content-hash redaction makes the §13.13 purge transaction set every retained `llm_calls.input_hash` and `llm_calls.output_hash` to `NULL`, keeping only values not derived from owner content bytes. Rejected alternatives: unconditional retention — deterministic hashes are a candidate-confirmation oracle for guessable purged content; keyed or salted hashing — the key shares the workspace, so the oracle survives; affected-closure-only redaction — a missed content path fails open, and the reset is already global.
- 2026-07-12 — PR #68 review: exact-recomputation identity is the run execution configuration (`provider`, `model`, `prompt_policy_hash`) plus call `input_hash`; the §12.15 hash byte form is pinned to code-point-sorted keys, UTC `Z` datetimes with exactly six fractional digits, raw-UTF-8 strings with mandatory-only escapes, integer-only numbers, and SHA-256 lowercase hex, while storage offset retention and deeper Unicode semantics remain deferred to issue #56. Rejected alternatives: payload/policy-only equivalence — cross-model attempts would classify as exact repeats; renaming the match to a payload/policy match instead of fixing the key — recomputation identification is the telemetry's stated purpose; leaving the byte form per-implementation — §12.15's cross-implementation hash-equality claim would be false.
- 2026-07-12 — PR #68 review: `GapQuestion.question` is bounded at 1,024 UTF-8 bytes in §11's boundary-limit block, so the §14.7 copied `question_text`/`question_reason` metadata object stays under 2.2 KiB at maximal canonical escaping and every persisted gap remains answerable within the 4 KiB entity-metadata budget; an oversized Stage 4 candidate question is invalid structured output. Rejected alternatives: raising the metadata budget — it weakens a generic every-entity bound to accommodate one producer; copying a hash or truncated excerpt instead of the text — the answer would stop being self-contained interpretable evidence after question supersession or purge.
- 2026-07-12 — PR #68 review: every accepted `datetime` value is offset-aware — an ISO 8601 string must carry `Z` or numeric `±hh:mm`, and a naive value fails validation at transport, hydration, and direct construction — so the §11 hash rule's UTC normalization is total; storage offset retention and workspace timezone ergonomics stay deferred to issue #56. Rejected alternatives: interpreting naive values as UTC at hash time only — ambiguous stored semantics would persist and silently misstate non-UTC instants; interpreting them in a workspace or platform timezone — the same accepted bytes would hash differently across environments, breaking §12.15's cross-implementation stability.
- 2026-07-12 — PR #68 review: `OccurredAt` bounds are offset-aware `datetime` values at every precision — §23's capture example now carries offset-aware midnight bounds, and §11.1 states that coarse-precision time-of-day is representational with `precision` alone carrying the temporal meaning. Rejected alternatives: a naive date-only exception for coarse precisions — it reintroduces the undefined-UTC-normalization divergence the offset-aware rule just closed; retyping coarse bounds as `date` fields — a second temporal shape would ripple through §12 storage, §15 serialization, §16.7 width computation, and the pinned hash byte form while adding no meaning beyond the existing `precision` discriminator.
- 2026-07-12 — PR #68 review: resume-branch replacement identity is the NFC case-folded branch name — fold-equal generation supersedes the matching current branch, `--branch` selectors resolve by folded identity, and two current branches never fold equal, so `out/<branch>/` directories cannot collide or alias on case- or normalization-insensitive filesystems; SQLite cannot fold in an index, so like Stage 6 view identity the canonical check is the Stage 10 transaction under the writer lock and the raw-name partial unique index stays as the exact-spelling backstop. Rejected alternatives: a stored service-folded name column with a unique index — duplicated identity state that SQLite cannot re-verify and that can drift from `name`; folded or encoded branch export directories — collisions are already impossible once identities cannot fold equal, and deeper branch-path hardening belongs to the #47 export-writer work.
- 2026-07-12 — PR #68 review: every fact's selections include at least one effective-record evidence item — a displaced-record support descriptor is additional scoped support, never the sole source — enforced as invalid structured output in §15.2 and atomically at Stage 3 commit, so each fact's provenance closure hydrates at least one effective record that states it. Rejected alternatives: keeping the prompt-contract prohibition without a structural check — an only-descriptor fact §15.2 already forbids would persist undetected and verify against no stated content; forbidding descriptor selection entirely — it reopens the §9.4 corrected-import `high` ceiling #64 deliberately preserved.
- 2026-07-12 — PR #68 review: the §13.3 governing record is per fact — the latest by (`recorded_at`, ID) of the effective records the fact selects, total by the rule-6 effective-selection guarantee — so sibling corrections with different `OccurredAt` or `project` govern only the facts that select them; displacement, effective records, and descriptors remain pre-call service computations, and each candidate's governing record is checked deterministically at commit. Rejected alternatives: the lineage-wide latest-effective rule — it leaks the latest sibling's placement and project onto facts stated only by the other sibling, misclassifying project views and temporal provenance; rejecting or merging siblings with conflicting provenance — sibling coexistence is #64's semantics and a merge requires exactly the persisted resolution artifact V1 rejected.
- 2026-07-13 — PR #68 review: the displaced-record support descriptor excludes `title` — it serializes exactly `id`, `raw_log_id`, `strength`, `uri`, and `path`, because §11.3 leaves `title` unconstrained free text that can carry source-derived prose (a commit subject or artifact name), which would leak displaced wording back into §15 prompts; `uri`/`path` remain structural locators under §29.3 disclosure and §29.4 pre-serialization ignore re-checks. Rejected alternatives: normatively restricting `title` to a non-content label — it constrains stored data and every future producer for one projection's benefit and still needs migration of already-captured titles; keeping `title` with prompt-side discipline — exactly the non-deterministic exclusion #64 rejected.
- 2026-07-13 — PR #68 review: §15 inputs are §11 shapes exactly as each receiving contract declares them — complete persisted shapes or declared narrower projections (the §15.6/§15.7 parsed job-description view without `raw_text`, §13.3 rule 10 support descriptors) — and a declared projection may not be widened toward the complete entity; the blanket complete-shape sentences in §11/§12 read as forcing `JobDescription.raw_text` into resume prompts against §15.6/§15.7 and §29.3's disclosed surface. Rejected alternatives: passing complete entities everywhere — vacancy raw text would transit resume calls for no contract need, widening §29.3; enumerating every projection centrally in §11 — §15 and §29.3 already own the per-contract surface, §11 only forbids widening.
- 2026-07-13 — PR #68 review: the §15.9 parser input carries no `JobDescription.id` — the input is the owner-supplied vacancy payload, no entity exists until the atomic post-parse persist (§13.8), and §11's post-response ID assignment applies uniformly, so §29.3's parser row narrows to vacancy raw text. Rejected alternatives: pre-allocating the entity ID before the call — a stage-specific exception to §11's post-response rule for an input field the parser never uses; capture-then-parse persistence so the ID legitimately predates the call — §11.11 requires `parsed`, so an unparsed row cannot validate, and a failed parse would strand a half-entity.
- 2026-07-14 — Issue #29, following the "Owner pre-decision (2026-07-14)" issue comment: the repository license is MIT — code will land in this repository, and portfolio consistency across the selfos ecosystem wins over spec-only licensing; the public README states the design-specification stage plainly and itself defers the security policy to the selfos umbrella repository by link until code lands — no SECURITY.md file is added at the spec stage (§26.1). Rejected alternatives: CC-BY-4.0 as a spec-only license — the repository will not stay spec-only, and a later relicense of mixed contributions is avoidable friction; per-repo bespoke licensing — ecosystem consistency is the stated deciding criterion.
- 2026-07-14 — Issue #55, following the "Owner pre-decision (2026-07-14)" issue comment: every non-`init` command uses nearest-parent workspace discovery with an explicit no-fallback `--workspace` override and hard failure outside a workspace; destructive or cost-bearing commands require `--yes` under non-TTY stdin; `--json` stdout contains only one versioned closed result envelope while diagnostics use stderr; and a stable exit-code taxonomy separates completed semantic-negative `blocked` verdicts from operational failures. Rejected alternatives: current-directory-only discovery — breaks subdirectory invocation ergonomics and matches no VCS-like tool; config-file-defined exit codes or free-form result text — scripts and selfos need a stable machine contract; one shared generic error code — automation cannot distinguish retryable contention from integrity failure.
- 2026-07-14 — Issue #56, following the "Owner pre-decision (2026-07-14)" issue comment: every accepted datetime is offset-aware, ISO 8601 TEXT storage preserves the original offset supplied by the owner or source while every comparison and ordering uses the UTC instant, and §11's canonical hash form keeps equal instants hash-identical; the explicit workspace-configured IANA timezone, never ambient OS timezone or locale, resolves naive CLI input, `today`, and retrospective anchors, with DST gaps and folds failing closed; calendar anchors use ISO Monday-start weeks and Q1 January–March; V1 generated and verified output is English, any-language source voice remains byte-exact, Russian-source-to-English-facts evaluation is mandatory, and localized generated output is deferred; Unicode identity uses NFC plus locale-independent default case folding only at the named scope-target, project-match, view-slug, and branch points; and V1 local paths use POSIX Linux/macOS semantics with Windows drive-letter, UNC, and backslash forms rejected. Rejected alternatives: UTC-normalizing storage — loses the owner or source's original offset and degrades recorded-at and occurred-at provenance; ambient OS timezone — lets the same workspace persist different instants on different machines; multilingual verified output in V1 — §16.3, §16.9, and §16.10 have no verified non-English coverage, so claiming it would be dishonest; Windows path support — is untestable on the V1 platform set and widens §29.4's attack surface.
- 2026-07-14 — Issue #51, following the "Owner pre-decision (2026-07-14)" issue comment and the same issue's "Handoff coverage: P2 + P4" comment: transport failure, §15.1 response-validation failure, and valid negative semantic results are normatively separate; transport retry is bounded, foreground-only, jittered, limited to enumerated retryable failures, and subject to a total deadline; context overflow fails closed with no V1 partition/merge and no truncation; multi-call stages persist no partial business result, reuse no partial valid response across runs, and rerun whole with the repeated cost accepted; cancellation leaves the prior current generation or §13.13 invalidated state; config-owned budgets have conservative defaults and privacy-safe cost reporting; and the existing telemetry columns carry a stable transport/runtime failure-code vocabulary from the first runner implementation. Rejected alternatives: deterministic partition/merge for complete-set stages — typed intermediate contracts, cross-partition contradiction detection, and global completeness checks are a large correctness surface V1 does not need, while fail-closed recovery is honest and available at the owner level; response caching across runs for partial batch retry — a cache is business-adjacent state with staleness and invalidation semantics against current-generation invariants for a cost saving the owner accepted; truncate-to-fit or model-side sampling — silently violates the completeness and provenance invariants §13 and §15 exist to protect; unbounded provider-SDK default retries — create unbounded foreground stalls and hidden cost, while §29.2 forbids background completion.
- 2026-07-14 — Issue #48, following the "Owner pre-decision (2026-07-14)" issue comment: JD/workspace lifecycle adds `jd list`, `jd show`, and `jd delete` with privacy-first in-transaction purge of dependent branches, bullets, and findings plus managed-output removal — never FK-blocked and with no recompute — plus `workspace purge`, which removes every managed content class including all telemetry and backups while retaining secret-free configuration. Rejected alternatives: FK-blocking JD deletion until branches are manually removed — a privacy operation must not be blockable by derived state, by the same §5.3 principle as raw-log deletion; cascade-less soft retention of JD raw text — third-party personal data has no owner value once deleted; selective telemetry deletion on point deletes — §13.13 rule 5's oracle and fail-open argument requires global content-hash redaction with telemetry retained instead.
- 2026-07-14 — Issue #48, following the "Owner pre-decision (2026-07-14)" issue comment: erasure posture and secrets require `secure_delete` on every connection, WAL checkpointing after each destructive flow, and `VACUUM` on workspace purge; credentials are environment/keyring references only, literal credential values in config fail closed, and snapshots, wear-leveling, swap, provider retention, and external copies are stated as outside application control. Rejected alternatives: plaintext keys in `config.toml` as an accepted risk — contradicts §29.4's transport-only rule and makes the workspace a secret store; `VACUUM` after every point deletion — full-database rewrite cost on the common privacy operation is disproportionate while checkpointing bounds live WAL retention and purge covers the strong case; claiming physical-erasure guarantees — dishonest against SSD and snapshot reality.
- 2026-07-15 — Issue #52, following the "Owner pre-decision (2026-07-14)" issue comment: integration-envelope identity is (`source_system`, `source_record_id`) with content-hash idempotency; all-or-nothing batches are bounded by §11's payload limit and safely retried by rerunning the file; and external contract versioning is an acquisition-boundary concern distinct from §12.14 database migration. Rejected alternatives: per-record partial batches — a half-imported export file is exactly the ambiguous partial state §13 forbids elsewhere, while rerun idempotency makes all-or-nothing cheap; last-write-wins on changed upstream content — silently rewrites evidence history against §5.3; treating upstream corrections as in-place updates — the owner's §14.4 correction flow is the only reinterpretation channel.
- 2026-07-15 — Issue #33, following the "Owner pre-decision (2026-07-14)" issue comment: GitHub import identity is `repo@full-40-hex-SHA` under the §19.4 envelope; author/committer identity and authored/committed timestamps are typed payload fields; owner attribution is an explicit conservative field defaulting to `unknown`, and only `owner` yields `commit_or_pr` strength while `unknown`/`not_owner` import as `artifact_reference`. Rejected alternatives: inferring attribution from git author email/login matching — a heuristic identity join Exp2Res cannot verify locally and exactly the silent promotion §5.10 forbids; accepting abbreviated SHAs — prefix collisions make the idempotency key unstable; keeping `commit_or_pr` strength for unknown attribution — a teammate's commit would calibrate as owner work product.
- 2026-07-15 — Issue #28, following the "Owner pre-decision (2026-07-14)" issue comment and its ratification of the issue's earlier owner comments: direct ephemeris intake is confined to source-tagged activity-domain evidence, so a learning event's time aspect may arrive as activity evidence but its knowledge aspect routes through Atlas; the selfos-side adapter owns Tick-like wire-format and recorded-at/occurred-at mapping while Exp2Res stays source-agnostic; Atlas snapshots carry knowledge state on Atlas's own scales, trail segments, and evidence references into `RawLog` plus `EvidenceItem` with `knowledge_state_snapshot` strength, calibrated for studied/learning attribution but not built/production support and never imported as a ready-made `SelfSignal` or `SelfClaim`; the pre-implementation `atlas_artifact_ref` entry type is retired in favor of `atlas_snapshot` with no §12.14 data migration because no implementation or persisted data exists; §30 defines loopback-URL-only, config-coupled mirror and JD-to-resume views as a deferred post-CLI iteration, with export refusal rendered as a first-class outcome and current unanswered GapQuestions exposed through managed `out/assessment/<view>/gap_questions.md`, while answers return only as ordinary diary raw logs with no link-back; and a future `private` RawLog marker is reserved, not implemented in V1, to exclude that log from every LLM-stage input, including user-initiated runs, at the §15 input-assembly boundary. Rejected alternatives: direct learning-event knowledge imports — knowledge must route through Atlas to keep one knowledge home; ready-made signal or claim imports — §5.10 requires explicit promotion inside Exp2Res; a coupled shell API — mutual blindness requires URL/file-only embedding without consumer knowledge.
- 2026-07-15 — Issue #47, following the "Owner pre-decision (2026-07-14)" issue comment: managed-output directories are derived only from opaque service-assigned snapshot and branch IDs, while closed manifests carry assessment-view identity, snapshot title, branch display name, job-description identity, and a canonical hash of the exact render-input state; this supersedes Issue #37's name-keyed scheme, retires percent-encoded view slugs and the `assessment` branch-name reservation because names are no longer paths, and simplifies §13.13 rule 10 ownership to captured ID-keyed directories with no folded-name or canonical-path sparing; no migration is required because the retired scheme was never implemented. Rejected alternatives: keeping slug paths with hardening — every encoding, reservation, and fold-collision rule fought the root cause that user text was a path; per-view stable name directories — stability now comes from manifest-keyed removal, and IDs are stabler than any name.
- 2026-07-15 — Issue #47, following the "Owner pre-decision (2026-07-14)" issue comment: managed-output publication uses a same-filesystem temp sibling, writes and flushes the complete member set with the closed manifest last, makes one successful final-entry atomic rename or exchange the publication point, and applies canonical-root containment plus no-follow checks to every managed write, rename, enumeration, read, and delete; where exchange is unavailable, a reserved rollback sibling makes the temporary no-current-set interval recoverable, and the next writer deterministically reconciles abandoned candidates and rollbacks without letting their failure block a privacy deletion. Rejected alternatives: in-place overwrite — a crash mid-set leaves a current-looking partial export; lockfile-only discipline — it does not survive a crash; following symlinks with checks — no-follow is strictly safer and managed directories have no legitimate symlinks.
- 2026-07-15 — Issue #43 cluster 1, following the "Owner pre-decision (2026-07-14)" comment on #43: the §13 header's named stale-export invalidation rule is the single normative home for managed-export staleness mechanics with a closed three-trigger list — supersession, Stage 7/11 verifier-state change, gap answer — while §13.7/§13.11/§14.7/§16.11 keep only trigger and affected sets; same-view publication replacement and the abandoned-sibling preamble consolidate into §13.14 rule 5 with §8.1/§13-header pointers. Rejected alternative: leaving per-site mechanics — five copies of enumerate/remove/report drift independently, and the header's trigger list was already incomplete because the verifier-state-change trigger lived only in the stage sites.
- 2026-07-15 — Issue #43 cluster 2: §11's preamble is the sole semantic home of the supersession lifecycle and its per-entity replacement identities; §9.1's ontology overview becomes reference-only over it, while §12 rule 9 and the §13 header remain the enforcement-layer statements they already are. Rejected alternative: also trimming the §12/§13 layer statements — they are the deliberate query- and stage-level enforcement architecture, not paraphrase.
- 2026-07-15 — Issue #43 cluster 3: §5 returns to the philosophy layer — §5.3 cites §13.13's purge/rebuild and rerun/invalidation lifecycle instead of restating it and drops the `deletion_incomplete` machine code from principle prose, §5.9 and §11.9 defer the contradiction lifecycle to §13.4 as the single authority, §16.1/§13.12/§18 cite §13.13 rules 5–6 instead of restating the purge, and §25 gains a §27-style reference-only charter. Rejected alternative: keeping operational detail in the principle layer — principle-side copies must track every lifecycle refinement (for example #63's retention rule) or silently mislead, which is the drift class #43 targets.
- 2026-07-15 — Issue #43 cluster 5a: §11.14 is the single normative home of the verification-finding/advisory-rewrite inspect-only lifecycle — persisted only in finding history, never applied, never a later prompt input, never exported — and the six restating sites (§13.7, §13.11, §14.9, §14.10, §15.5, §15.7) keep only their site-specific halves: one-pass/no-writer stage rules, replacement-generation requirements, and owner presentation, plus pointers. Rejected alternative: a stage-rule canonical home in §13.7/§13.11 — the rule is a field-lifecycle property of the persisted §11.14 entity, and two parallel stage homes would still be two homes.
- 2026-07-15 — Issue #43 clusters 5b/5d: §9.4 remains the sole calibration home — §15.2/§15.3 cite its ceiling, conflicting-context cap, and propagation caps instead of restating the parameters — while §11.7's answered-gap paragraph defers to §14.7/§17's owning semantics and the known-gap-assertion rule stays canonical at §13.6. Rejected alternative: keeping cap parameters inside the §15 contracts as self-contained prompt text — §15 contracts already cite § rules throughout, and duplicated parameters (two-distinct-raw-logs, the `medium` counter-fact cap) are exactly the multi-home drift #43 targets.
- 2026-07-15 — Issue #54, following the "Owner pre-decision (2026-07-14)" issue comment and the same issue's P8 handoff comment: V1's product-facing export artifact is a verified bullet pack with closed versioned companion schemas and deterministic byte-identical rendering; the full resume document model is deferred to a post-mirror iteration; and cross-bullet coherence uses deterministic planning, never §15.6 writer-context widening or a second LLM coherence pass. Rejected alternatives: shipping "resume export" naming for a bullet artifact — the product would claim a document it does not produce, violating §5's honesty stance toward its own surface; an LLM coherence/dedup pass — it reintroduces the cross-bullet fabrication surface §15.6's isolation exists to prevent; open-shape companions — unversioned JSON becomes an implicit contract consumers guess at.
- 2026-07-15 — SDD version 0.3, status "Draft / implementation-oriented" → "Implementation-ready": #59's definition-of-ready holds — gates A–C landed via PRs #66–#68 and #83–#85, the #43 dedupe pass via PR #86, and the Decision Log/§21/§24 meta-gates verified (111 dated decisions before this entry, 49 evals, 52 acceptance criteria matching the map). Spec changes continue through issues and this log; the version marks readiness, not freeze. Rejected alternative: bumping to 1.0 — the spec is implementation-ready, not implementation-proven; 1.0 belongs after the V1 slices validate the contracts.
- 2026-07-15 — Issue #80: vendored the selfos-skills SDD-conventions template v1.0.0 as a generated AGENTS.md block and the Decision Log lint 1.1.0 into scripts/, with one repository-owned aggregate check in offline CI and a 2026-07-15 grandfather baseline for pre-adoption entries. Rejected: referencing selfos-skills at runtime — a fresh offline checkout must not depend on another repository or the network for its own correctness rules. #80
- 2026-07-15 — Issue #72, following the "Owner pre-decision (2026-07-14)" issue comment: §15.11 is the normative field-ownership matrix for every §15 transport field, and deterministic model echoes leave the output shapes — Stage 3 copies fact `project` and derives `source_log_ids` with `occurred` now a nullable narrowing-only channel, Stage 6 service-copies the snapshot summary from the `narrative_summary` claim and service-populates `gap_question_ids`, and Stage 10 derives bullet `source_log_ids` and service-sets `source_self_claim_ids`; retained apparent echoes (non-null `occurred`, bullet `source_fact_ids`, verifier quotations) document their semantic rationale in the matrix. Rejected alternative: keeping deterministic echoes as an independent cross-check — equality against a service-derivable value validates no model reasoning and only adds invalid-output retry paths.
- 2026-07-15 — Issue #94, surfaced by the #72 audit and the PR #93 review: Stage 6 derives `AssessmentSnapshot.title` deterministically from the assessment-view identity — the renderer-owned literal `Self-Assessment — Global` or `Self-Assessment — <canonical scope_target>` — so the required §11.7 field has exactly one producer, equal views derive equal titles, and no unverified prose channel opens one field over from `summary`. Rejected alternatives: a model-authored §15.4 title — reopens the unverified snapshot-prose escape hatch §13.6 closes for `summary`; an owner `--title` flag — new CLI surface for a fully derivable value with §16.12 voice-scoping questions and no owner value.
